---
slug: workstation-tutorial-4-mcp-server
title: "Workstation Setup leerlijn — Deel 4: De MCP-server aansluiten"
contentType: tutorial
authors: [conduction]
date: 2026-05-29
summary: Een complete Conduction-workstation heeft minstens één MCP-server aangesloten in de project-root. Deze korte module legt uit wat MCP is, hoe het .mcp.json-bestand werkt, en hoe je de Playwright-browserpool opzet waar de testing-skills van afhangen. Inclusief verwijzingen naar de aparte Conduction MCP-server-tutorial. Vierde van zes korte modules.
tags: [Workstation, MCP, Playwright, Tutorial series]
apps: []
durationMinutes: 15
series: workstation-tutorial
partNumber: 4
---

import {Outcomes, Outcome, Prerequisites, PrerequisiteItem, Troubleshooting, TroubleshootingItem, NextSteps, NextStep, ContactCta} from '@conduction/docusaurus-preset/components';
import Details from '@theme/Details';

MCP — **Model Context Protocol** — is hoe Claude Code met de buitenwereld praat: browsers, GitHub, Slack, je lokale Nextcloud, de Conduction MCP-server. Een workstation is pas echt af als er minstens één MCP-server via `.mcp.json` in je project-root is aangesloten. Dit deel behandelt de basis en levert je een werkende Playwright-browserpool op. De aparte installatie van de **Conduction MCP-server** staat in zijn eigen tutorial — daar linken we onderaan naar.

{/* truncate */}

<Outcomes title="Wat je oplevert">
  <Outcome>Begrip van wat MCP is, waarom het bestaat, en waar <code>.mcp.json</code> hoort te staan.</Outcome>
  <Outcome>Een <code>.mcp.json</code>-bestand in je project-root met de Playwright-browserpool — zes headless browsers + één headed — waar de Conduction testing-skills van afhangen.</Outcome>
  <Outcome>De browsers verbonden en geverifieerd in het MCP-servers-paneel van VS Code.</Outcome>
  <Outcome>Een wegwijzer naar de aparte Conduction MCP-server-tutorial voor de volgende stap.</Outcome>
</Outcomes>

<Prerequisites title="Wat je nodig hebt">
  <PrerequisiteItem><a href="/nl/academy/workstation-tutorial-3-claude-code">Deel 3</a> gedaan — Claude Code is ingelogd en de globale safety hooks zijn live.</PrerequisiteItem>
  <PrerequisiteItem>Een projectmap waar je een <code>.mcp.json</code> in kwijt kunt. Elke Conduction-repo (bijv. <a href="https://github.com/ConductionNL/openregister">openregister</a>) werkt; een leeg <code>git init</code>-mapje is ook prima voor de oefening.</PrerequisiteItem>
  <PrerequisiteItem>Ongeveer 15 minuten.</PrerequisiteItem>
</Prerequisites>

## In één zin

MCP is een klein open protocol waarmee Claude Code **naar buiten** kan bellen naar externe services via een uniforme interface. Een "MCP-server" is elk programma dat dat protocol spreekt; Claude behandelt het als een tool-leverancier. Je hangt servers aan een project door ze op te sommen in `.mcp.json` in de project-root.

Een paar servers die je bij Conduction tegenkomt:

- **`browser-1` … `browser-7`** — Playwright-browserinstanties die Claude voor testing-skills kan gebruiken (`/test-app`, `/test-counsel`, de `hydra-gate-*`-familie).
- **De Conduction MCP-server** — live toegang tot interne Conduction-systemen terwijl je werkt. Behandeld in een eigen tutorial.
- **Nextcloud MCP** — directe toegang tot een draaiende Nextcloud-instantie vanuit Claude. Optioneel.

Deze module stopt bewust bij de Playwright-pool: het is de pool die elke Conduction-developer nodig heeft, en hij werkt zonder extra accounts.

## Stap 1: maak `.mcp.json` in de project-root

In een Conduction-project bestaat het bestand al — open het, scroll, controleer. In een nieuwe map maak je hem zelf aan. De minimumconfiguratie waar alle testing-skills van uitgaan is de **zeven-browserpool**: zes headless browsers voor parallelle agents, één headed browser voor wanneer je wilt meekijken.

`/pad/naar/je-project/.mcp.json`:

```json
{
  "mcpServers": {
    "browser-1": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser", "chromium", "--headless", "--isolated"]
    },
    "browser-2": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser", "chromium", "--headless", "--isolated"]
    },
    "browser-3": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser", "chromium", "--headless", "--isolated"]
    },
    "browser-4": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser", "chromium", "--headless", "--isolated"]
    },
    "browser-5": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser", "chromium", "--headless", "--isolated"]
    },
    "browser-6": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser", "chromium", "--isolated"]
    },
    "browser-7": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--browser", "chromium", "--headless", "--isolated"]
    }
  }
}
```

Let op de asymmetrie: **browser-6 laat `--headless` weg**. Dat is bewust — het is de zichtbare browser voor wanneer je Claude wilt zien werken. De andere zes blijven onzichtbaar.

> **Hoef je niet vanaf nul te typen.** De `.github`-repo bevat een [voorbeeld-.mcp.json](https://github.com/ConductionNL/.github/blob/main/docs/claude/examples/.mcp.json.example) die je direct kunt kopiëren.

## Stap 2: vertrouw de servers via `.claude/settings.json`

Elke Conduction-repo heeft een `.claude/settings.json` (op projectniveau — los van je globale) die de MCP-servers vooraf goedkeurt zodat achtergrondagents niet door prompts geblokkeerd worden. De twee relevante regels:

```json
{
  "enableAllProjectMcpServers": true,
  "permissions": {
    "allow": ["mcp__browser-*"]
  }
}
```

- **`enableAllProjectMcpServers: true`** — keurt alle servers in `.mcp.json` automatisch goed, zonder prompt bij elke reload.
- **`mcp__browser-*`** — keurt elke browser-toolaanroep vooraf goed, zodat sub-agents die parallel werken niet stilzwijgend worden geweigerd.

In een lege map zet je deze settings zelf neer. In een Conduction-repo staan ze er al.

## Stap 3: VS Code opnieuw laden

`Ctrl+Shift+P` → **"Developer: Reload Window"**. VS Code leest `.mcp.json` opnieuw in en start de browser-processen.

## Stap 4: verifiëren

Open het MCP-servers-paneel in VS Code. Twee gelijkwaardige manieren:

- Typ **`/MCP servers`** in het Claude Code-chatvenster, of
- `Ctrl+Shift+P` → zoek **"MCP servers"** → open het paneel.

Alle zeven browsers moeten **Connected** tonen. Als er één een fout geeft, open dan het output-paneel: `Ctrl+Shift+P` → **"Output: Focus on Output"** → kies **Claude VSCode** in de dropdown — de foutregel vertelt precies wat er misging.

> **Veelvoorkomende oorzaak als een browser niet verbindt:** de Playwright Chromium-binary is niet geïnstalleerd. Draai `npx playwright install chromium` vanuit je WSL-terminal (zou er al moeten staan uit Deel 2 Stap 9). Reload het venster daarna.

## Hoe weet Claude welke browser hij moet gebruiken?

Er is geen centrale dispatcher. De skills zelf wijzen browsers toe — bij conventie:

| Browser | Wanneer wordt hij gebruikt |
| --- | --- |
| `browser-1` | De standaard; je hoofdsessie gebruikt deze. |
| `browser-2`…`browser-5` en `browser-7` | Parallelle sub-agents (bijv. `/test-app` Full mode, `/test-counsel`). |
| `browser-6` (**headed**) | Wanneer je Claude expliciet hebt gevraagd om "laat me het zien" — dat venster popt op zodat je kunt meekijken. |

Als een browser een fout geeft, valt Claude terug op de volgende genummerde. Je hoeft dat niet handmatig te regelen.

## Waarom zeven browsers?

Een pragmatische keuze uit een paar jaar interne praktijk:

- Eén standaard + vijf parallelle sub-agents = zes headless. Dat dekt het zwaarste parallelle scenario in `/test-app` Full mode (vijf gelijktijdige sub-agents plus de orchestrator).
- Eén headed browser voor observatie — handig wanneer je *wilt zien* wat Claude doet in plaats van alleen het resultaat lezen.

Andere workspaces gebruiken misschien minder (of meer) browsers afhankelijk van het parallelisme. De Nextcloud-workspace gebruikt deze zeven; voor een eenmalig prototype kun je gerust naar twee terug.

## En de Conduction MCP-server dan?

Voor Conduction-projecten is de Playwright-pool alleen niet genoeg. De **Conduction MCP-server** — een server die we intern draaien — geeft Claude live toegang tot de systemen waarmee we bouwen: registers, catalogi, het Hydra-jobbord, en meer.

Het is een aparte installatie met een eigen auth en configuratie. We hebben hem in een eigen tutorial gezet zodat deze kort blijft:

- **Tutorial (gepland):** [Zet de Conduction MCP-server op](https://github.com/ConductionNL/.github/issues/55) — zie issue #55 voor status.

Zodra die tutorial live is, voeg je nog een entry toe aan dezelfde `.mcp.json` en reload je. De Playwright-pool blijft staan.

## Een noot over `.mcp.json` vs. `~/.claude/settings.json`

Twee bestanden, verschillende scopes. Houd ze uit elkaar:

| Bestand | Scope | Wat het doet |
| --- | --- | --- |
| `~/.claude/settings.json` | **User-niveau**, alle projecten | Je safety-policy, write-approval hooks, version-check (Deel 3). |
| `<project>/.mcp.json` | **Project-niveau**, deze repo | Welke MCP-servers gestart worden voor dit project. Browsers, de Conduction MCP-server, alles wat verder specifiek is. |
| `<project>/.claude/settings.json` | **Project-niveau**, deze repo | Projectpermissies, MCP-allowlist, vooraf-goedkeuringen. |

De user-configuratie bevat nooit MCP-servers; de project-configuratie wel. Door die scheiding kiest elk project zijn eigen set servers zonder dat je globale safety-policy in de weg zit.

<Troubleshooting title="Probleemoplossing">
  <TroubleshootingItem symptom="Het MCP-servers-paneel toont 0 verbonden, zonder foutmelding">
    VS Code heeft <code>.mcp.json</code> niet opgepikt. Reload het venster (<code>Ctrl+Shift+P</code> → Developer: Reload Window). Nog steeds leeg? Controleer dan of het bestand in de project-root staat en geldige JSON is — plak het in <a href="https://jsonlint.com">jsonlint.com</a>.
  </TroubleshootingItem>
  <TroubleshootingItem symptom="Eén specifieke browser geeft een fout 'browserType.launchPersistentContext: Executable doesn't exist'">
    De Playwright Chromium-binary is niet geïnstalleerd. Draai <code>npx playwright install chromium</code> vanuit je WSL-terminal, en reload daarna VS Code.
  </TroubleshootingItem>
  <TroubleshootingItem symptom="Een achtergrondagent zegt 'tool call denied' voor mcp__browser-3">
    De <code>.claude/settings.json</code> van het project heeft niet de <code>mcp__browser-*</code> allow-regel, of <code>enableAllProjectMcpServers</code> ontbreekt. Voeg beide toe, reload, probeer opnieuw.
  </TroubleshootingItem>
  <TroubleshootingItem symptom="browser-6 start headless ook al heb ik --headless verwijderd">
    Controleer of er geen <code>HEADLESS=true</code> in je shell-omgeving staat en dat geen andere <code>--headless</code>-flag is toegevoegd door een extensie-config. Reload en kijk in het output-paneel mee tijdens de start om de doorgegeven args te bevestigen.
  </TroubleshootingItem>
</Troubleshooting>

## Test jezelf

Vijf korte vragen om te checken of je dit deel begrepen hebt. Vastgelopen? Klik **Hint**. Curieus naar het antwoord? Klik **Antwoord**.

<div className="tutorial-quiz">

**1. Waar staat MCP voor, en wat is zijn rol binnen Claude Code?**

<Details summary="Hint">

Drie letters. Het middelste woord is wat elke externe tool die Claude raakt nodig heeft om te delen.

</Details>

<Details summary="Antwoord">

**Model Context Protocol.** Het is het kleine open protocol waarmee Claude Code via een uniforme interface externe services kan aanroepen — browsers, Slack, GitHub, je lokale Nextcloud, de Conduction MCP-server.

Alles wat MCP spreekt, kan via een project-`.mcp.json` worden aangesloten en Claude kan het als tool gebruiken. Zonder MCP zou elke externe integratie een eigen adapter nodig hebben; met MCP zien ze er allemaal hetzelfde uit voor Claude.

</Details>

</div>

<div className="tutorial-quiz">

**2. Waarom heeft de standaard Conduction-pool *zeven* browsers, en waarom is er één headed?**

<Details summary="Hint">

Zes headless dekken een specifiek parallelscenario; de zevende heeft een ander doel.

</Details>

<Details summary="Antwoord">

**Zes headless browsers** dekken het zwaarste parallelle scenario in `/test-app` Full mode: één orchestrator-sessie plus vijf gelijktijdige sub-agents, elk op een eigen browser, plus één reserve (`browser-7`). De Hydra-pipeline en `/test-counsel` leunen op dezelfde pool.

**De zevende browser is headed** (`browser-6` — let op: genummerd, niet de laatste) omdat je soms *wilt zien* wat Claude doet in plaats van alleen het resultaat lezen. Als je Claude vraagt om "laat me het zien" of "kijk mee", schakelt hij over naar dat zichtbare venster. Verder loopt al het echte werk op de zes headless.

</Details>

</div>

<div className="tutorial-quiz">

**3. Welke twee project-niveau settings keuren de MCP-browsers vooraf goed zodat achtergrondagents niet door prompts geblokkeerd worden?**

<Details summary="Hint">

Eén is een boolean die elke server in `.mcp.json` automatisch vertrouwt; de andere is een patroon in de permissions-allowlist.

</Details>

<Details summary="Antwoord">

In de `.claude/settings.json` van het project:

```json
{
  "enableAllProjectMcpServers": true,
  "permissions": {
    "allow": ["mcp__browser-*"]
  }
}
```

- **`enableAllProjectMcpServers: true`** keurt elke server in `.mcp.json` automatisch goed zonder prompt bij elke reload.
- **`mcp__browser-*`** keurt elke browser-toolaanroep vooraf goed, zodat achtergrond- en parallelle sub-agents hun toegewezen browser stilzwijgend kunnen gebruiken.

Zonder deze twee worden parallelle sub-agents stilzwijgend geweigerd en gebeurt er geen test — ook al ziet de rest er gezond uit.

</Details>

</div>

<div className="tutorial-quiz">

**4. Waar staat `.mcp.json`, en wat is het verschil met `~/.claude/settings.json`?**

<Details summary="Hint">

Twee bestanden, twee scopes. De één is per-user, de ander per-project.

</Details>

<Details summary="Antwoord">

`.mcp.json` staat in de **project-root** (naast `package.json` of `composer.json`). Het is per-project: welke MCP-servers moet dit project starten — browsers, de Conduction MCP-server, wat dan ook.

`~/.claude/settings.json` is **user-niveau**: je safety-policy, write-approval hooks, version-check. Geldt voor elk project dat je opent en bevat zelf nooit MCP-servers.

De splitsing is belangrijk omdat elk project zo zijn eigen MCP-servers kan kiezen zonder je globale safety-configuratie te raken. Het ene aanpassen verandert het andere nooit.

</Details>

</div>

<div className="tutorial-quiz">

**5. Hoe controleer je — in één oogopslag — dat de browsers echt live zijn nadat je `.mcp.json` hebt aangemaakt?**

<Details summary="Hint">

Er is een ingebouwd paneel voor. Je kunt het openen vanuit de Claude Code-chat of vanuit de command palette.

</Details>

<Details summary="Antwoord">

Open het **MCP-servers-paneel** in VS Code:

- Typ **`/MCP servers`** in het Claude Code-chatvenster, of
- `Ctrl+Shift+P` → zoek **"MCP servers"** → open het paneel.

Alle zeven browsers moeten **Connected** tonen. Staat er één op rood, open dan het output-paneel (`Ctrl+Shift+P` → "Output: Focus on Output" → kies **Claude VSCode**) en lees de foutregel. De meest voorkomende oorzaak is een ontbrekende Playwright Chromium-binary — los je op met `npx playwright install chromium`.

</Details>

</div>

<NextSteps title="Volgende stap" lede="De browsers zijn aangesloten. Tijd om een lokale Nextcloud te starten, zodat de apps die je straks bouwt ook ergens kunnen draaien.">
  <NextStep
    title="Deel 5 — Nextcloud lokaal draaien"
    href="/nl/academy/workstation-tutorial-5-nextcloud-lokaal"
    description="Een korte wegwijzer naar de bestaande Nextcloud-lokaal-tutorial, ingekaderd als de laatste installatiestap voordat je productie-klaar bent."
  />
  <NextStep
    title="Zet de Conduction MCP-server op (issue #55)"
    href="https://github.com/ConductionNL/.github/issues/55"
    description="De vervolgtutorial die de Conduction MCP-server toevoegt aan dezelfde .mcp.json. Wordt getrackt als issue #55 — lees het issue voor status en timing."
  />
  <NextStep
    title="De canonieke playwright-setup.md"
    href="https://github.com/ConductionNL/.github/blob/main/docs/claude/playwright-setup.md"
    description="De interne referentie-doc met de volledige uitwerking van de browserpool, het CLI-alternatief, en de gebruiksregels."
  />
</NextSteps>

<ContactCta
  title="Browser wil niet verbinden?"
  body="Stuur de exacte foutregel uit het Claude VSCode-output-paneel. De meeste browserfouten zijn een ontbrekende binary of een verouderde extensie-cache — beide vijfminuten-fixes zodra we de melding zien."
  cta={{ label: "Mail ons", href: "mailto:info@conduction.nl?subject=Workstation%20setup%20tutorial%20deel%204" }}
/>
