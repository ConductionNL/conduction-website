---
title: Partners, Conduction
description: Implementatie-, hosting- en integratiepartners voor het Conduction app-ecosysteem. Filter op tier en op de apps die je wilt uitrollen.
hide_table_of_contents: true
---

import {Section, SectionHead, PartnerDirectory} from '@conduction/docusaurus-preset/components';
import {usePartners, useBecomePartner} from '@site/src/data/partners-catalog';

export function PartnerDirectoryBlock() {
  const partners = usePartners();
  const becomePartner = useBecomePartner();
  return (
    <PartnerDirectory
      partners={partners}
      becomePartner={becomePartner}
      gridColumns={3}
    />
  );
}

<Section spacing="default">
  <SectionHead
    eyebrow="Partners"
    title="Implementatie, hosting, integratie."
    lede={<>De apps zijn open source en gratis. De meeste teams installeren en draaien ze zelf. Wil je hulp bij hosting, maatwerk-integraties of een uitrol in een gereguleerde sector? Onze gecertificeerde partners pakken het op. Ze draaien dezelfde <span className="next-blue">Nextcloud</span> als jij en bouwen op dezelfde apps.</>}
  />

  <PartnerDirectoryBlock />
</Section>
