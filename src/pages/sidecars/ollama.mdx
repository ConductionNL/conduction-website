---
title: Ollama sidecar, Conduction
description: Nextcloud sidecar wrapping Ollama for local LLM inference.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, CtaBanner} from '@conduction/docusaurus-preset/components';

<DetailHero
  appId="ollama"
  crumb={[{label: 'Sidecars', href: '/sidecars'}, 'Ollama']}
  status={{label: 'Sidecar', color: 'var(--c-cobalt-400)'}}
  version="upstream"
  locales="NL · EN"
  title="Ollama"
  tagline={<>A <span className="next-blue">Nextcloud</span> sidecar that wraps Ollama. Run local large-language models inside your workspace, install from the app store, keep your prompts on your own hardware.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docs.conduction.nl/ollama'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://github.com/ConductionNL/ollama-nextcloud'}}
  iconColor="var(--c-blue-cobalt)"
  icon={<svg viewBox="0 0 24 24"><path d="M12 3l9 5v8l-9 5-9-5V8z"/><circle cx="12" cy="12" r="2"/></svg>}
/>

<Section spacing="default">
  <SectionHead
    eyebrow="What it is"
    title="Ollama as a Nextcloud app."
    align="stack"
    lede="Ollama runs open-weight LLMs locally with a clean HTTP API. We package it as a Nextcloud-installable sidecar. You install the app, the upstream container runs alongside Nextcloud, and any app in the workspace can call the inference endpoint. Same upstream, less DevOps. Data never leaves the instance."
  />
</Section>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Why a sidecar"
    title="A sidecar is not a fork."
    align="stack"
    lede="We do not fork Ollama. The sidecar tracks upstream releases. The wrapper handles install, lifecycle, and the koppelvlak to Nextcloud. Models stay on the instance. Conduction is responsible for the integration, not for upstream Ollama."
  />
</Section>

<CtaBanner
  title="Run Ollama inside your Nextcloud."
  lede={<>Install from the Nextcloud app store. Wire local models into AI Companion. Drive summarisation flows from OpenConnector. Keep every prompt on your own hardware.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Talk to a partner', href: '/partners'}}
/>
