---
title: Install
description: Two minutes from the Nextcloud app store to a working Conduction app. Pick an app, drop it in, you're working.
hide_table_of_contents: true
---

import {Section, SectionHead, HowSteps, HowStep, AppsPreview, CtaBanner, Button, FAQ, FAQItem} from '@conduction/docusaurus-preset/components';
import PageHeading from '@site/src/components/PageHeading';

<PageHeading>Two minutes from app store to working install.</PageHeading>

<Section spacing="default">
  <SectionHead
    eyebrow="Install"
    title={<>Two minutes from app store<br/>to working install.</>}
    lede={<>Conduction apps are open source and on the official <span className="next-blue">Nextcloud</span> app store. You install them the same way you install any other Nextcloud app: as administrator, from the apps page, with one click. The schema bootstrap runs on first load, your team is writing data the same morning.</>}
  />

  <div style={{display: 'flex', gap: 14, marginTop: -16, flexWrap: 'wrap'}}>
    <Button variant="primary" size="lg" href="https://apps.nextcloud.com/categories/integration">Open Nextcloud app store</Button>
    <Button variant="secondary" size="lg" href="https://docs.conduction.nl/install">Read the install docs</Button>
  </div>
</Section>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="How install works"
    title="Three steps, in order."
    align="stack"
    lede="Same recipe whether you start with OpenRegister or OpenCatalogi. Most teams have all three steps done before lunch."
  />
  <HowSteps>
    <HowStep title="Open the Nextcloud app store.">
      Sign in as administrator, open the apps page, search for the Conduction app you want. The official packages live in the <strong>Integration</strong> category and ship signed, with semver tags.
    </HowStep>
    <HowStep title="Click install.">
      Nextcloud handles the rest: download, signature check, schema bootstrap, default-config seeding. The first request hits the freshly installed app within seconds; the schema bootstrap runs once and is idempotent.
    </HowStep>
    <HowStep title="Configure once, then write.">
      The supplied profiles cover most cases out of the box. WOO, ZGW, NLDS, BIO. Pick the profile that matches your sector, save, and your team is writing typed data the same morning.
    </HowStep>
  </HowSteps>
</Section>

<Section spacing="default">
  <SectionHead
    eyebrow="Pick an app"
    title="Three apps that ship the most outcomes."
    align="stack"
    lede="Start with the apps that solve a concrete problem on day one. They're all stable, GDPR-compliant, and free to install."
  />

  <AppsPreview
    apps={[
      {
        name: 'OpenRegister',
        tagline: 'Schemas, registers, structured data objects, the typed-data backbone for every app.',
        status: 'STABLE', version: 'v3.1',
        href: '/apps/openregister',
        icon: <svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="8" ry="3"/><path d="M4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6"/><path d="M4 12c0 1.7 3.6 3 8 3s8-1.3 8-3"/></svg>,
      },
      {
        name: 'OpenCatalogi',
        tagline: 'Public software catalog. Every app, dataset, API in your organisation, searchable in one place.',
        status: 'STABLE', version: 'v2.4',
        href: '/apps/opencatalogi',
        icon: <svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4"/><path d="M3 17l9 4 9-4"/></svg>,
      },
      {
        name: 'Integriq',
        tagline: <>Connect <span className="next-blue">Nextcloud</span> to anything, REST, SOAP, GraphQL, file drops, message queues.</>,
        status: 'BETA', version: 'v1.2',
        href: '/apps/openconnector',
        icon: <svg viewBox="0 0 24 24"><circle cx="6" cy="12" r="3"/><circle cx="18" cy="6" r="3"/><circle cx="18" cy="18" r="3"/><path d="M9 12h9M9 12l9-6M9 12l9 6"/></svg>,
      },
    ]}
    seeAll={{label: 'Browse all apps', href: '/apps'}}
  />
</Section>

<CtaBanner
  title="Need help getting started?"
  lede={<>Most teams install themselves. If you'd rather we run the rollout (especially for compliance-heavy sectors), our partners take it from "I want this" to "this is in production".</>}
  primaryCta={{label: 'Talk to a partner', href: '/partners'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docs.conduction.nl/install'}}
/>

<FAQ title="Install FAQ.">
  <FAQItem question="Do I need to be a Nextcloud administrator to install a Conduction app?" defaultOpen>
    Yes. Conduction apps install from the Nextcloud app store, which only the administrator role can access. Once installed, you grant per-user or per-group access through the standard Nextcloud app permissions screen. Regular users do not need any extra setup.
  </FAQItem>
  <FAQItem question="What Nextcloud version do I need?">
    Conduction apps target the latest two Nextcloud major versions. At time of writing that is Nextcloud 30 and 31. Older versions are supported on a best-effort basis. Check the per-app docs at docs.conduction.nl for the exact range for that release.
  </FAQItem>
  <FAQItem question="Can I install more than one Conduction app at the same time?">
    Yes, and most teams do. OpenRegister is the shared data foundation; once it is installed, every other Conduction app you install reads and writes through it without configuration. There is no per-app database, no extra plumbing, and no version-pin matrix between apps.
  </FAQItem>
  <FAQItem question="Does install need an internet connection or external services?">
    The install itself runs entirely inside your Nextcloud instance: download, signature check, schema bootstrap, default-config seeding. Some apps offer optional outbound integrations (AI features, document anonymisation, external mail). Each is opt-in and disabled by default until you configure a connector.
  </FAQItem>
  <FAQItem question="How do I uninstall, and what happens to my data?">
    Uninstall through the Nextcloud apps page. The default behaviour preserves data: registers, objects, and audit trails stay in OpenRegister, so you can reinstall later and pick up where you left off. To wipe data on uninstall, use the per-app cleanup option in the app's admin settings before you uninstall.
  </FAQItem>
  <FAQItem question="The app store does not show the Conduction app I want. What now?">
    Check that you are on a supported Nextcloud version (see above) and that your instance can reach apps.nextcloud.com. If it still does not appear, the app is most likely in pre-release. Pre-release builds are available from the GitHub releases page for that app, or your partner can pre-install them for evaluation.
  </FAQItem>
  <FAQItem question="Can I get an SLA, monitoring, or 24x7 support?">
    Yes, through a Conduction partner. Conduction itself ships the apps free; production support, SLAs, monitoring, and incident response are sold by partners listed on the support page. Public-sector clients can opt for our managed Common Ground tenant at commonground.nu instead.
  </FAQItem>
</FAQ>
