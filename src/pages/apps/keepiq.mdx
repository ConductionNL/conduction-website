---
title: Keepiq, Conduction
description: A password safe inside your Nextcloud. One vault per person, sharing per team, a place for the passwords your systems use. Encrypted so that not even your own admin can read them.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace, AppCrossLinks, FlowMock, BuildMock} from '@conduction/docusaurus-preset/components';
import {PartnersForApp} from '@site/src/components/PartnersFor';

<DetailHero
  background="cobalt"
  appId="doriath"
  crumb={[{label: 'Apps', href: '/apps'}, 'Keepiq']}
  status={{label: 'Beta', color: 'var(--c-orange-knvb)'}}
  version="v0.2.28"
  locales="NL · EN"
  title="Keepiq"
  tagline={<>A password safe inside your <span className="next-blue">Nextcloud</span>. One vault per person, sharing per team, and a place for the passwords your systems use.</>}
  intro={<>Keepiq (formerly Doriath) keeps the wifi code, the bank login and the supplier portal password where your team already works. Everything is encrypted so that not even your own admin can read it. Lose your master password and nobody, including us, can open your vault. Share a password with a team, take it back in one click, and get a warning before a certificate runs out. Open source, released under EUPL-1.2 and maintained by Conduction.</>}
  primaryCta={{label: 'Install from the Nextcloud app store', href: 'https://apps.nextcloud.com/apps/keepiq'}}
  secondaryCta={{label: 'Request a demo from a partner', href: '/partners/?app=doriath'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://github.com/ConductionNL/doriath'}}
  illustration={<AppMock app="doriath" />}
/>

<Section spacing="default">
  <div style={{display: 'grid', gridTemplateColumns: 'minmax(0, 1fr) 320px', gap: 'var(--space-8)', alignItems: 'start'}}>
    <div style={{minWidth: 0}}>
      <SectionHead
        eyebrow="What it does"
        title="Every password in one safe place. Never in a chat group again."
        align="stack"
        lede="Keepiq gives every person a vault, every team a shared folder, and every system its own entry. Search finds a password from the Nextcloud search bar. Sharing works with the users and groups you already have."
      />
      <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="4" y="10" width="16" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/></svg>}
      title="The wifi code, the bank login, the supplier portal. One vault."
    >
      Today the wifi code sits in a WhatsApp group and the bank login on a sticky note. Instead, every password lives in your own vault, in folders you choose. Type a name in the Nextcloud search bar and the entry is there. Copy it, use it, done. Nobody forwards a password by chat again.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M17 20h5v-2a4 4 0 0 0-3-3.87M9 20H4v-2a4 4 0 0 1 3-3.87M16 3.13a4 4 0 0 1 0 7.75M12 12a4 4 0 1 0 0-8 4 4 0 0 0 0 8Z"/></svg>}
      title="A colleague moves on. Every shared password is gone from their view."
    >
      Share a folder with a team and everyone in it can use the passwords inside. Add a password to the folder and the team has it. A colleague drops out of the group and their access ends by itself. Revoke one person in one click. No password reset round because someone walked out the door.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M22 2L11 13"/><path d="M22 2l-7 20-4-9-9-4z"/></svg>}
      title="Ask a supplier for a password. It never travels by mail."
    >
      Send a supplier a one-time link. They drop the password in, it lands encrypted in your vault, and the link is dead. Need to hand one out instead? Send a message that only the receiver can open and that destroys itself after reading. Nothing sits in plain text in anyone's inbox.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/></svg>}
      title="Certificates that warn you before they run out."
    >
      Keepiq keeps a list of every certificate with its end date and warns you before it expires. Certificates Keepiq issued itself renew in one click. Passwords get an expiry policy too, so the ones that should change every year get flagged when the year is up. Weak, reused and leaked passwords show up on the health page.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="M2 8h20"/><path d="M6 12h4"/></svg>}
      title="The passwords your systems use, with an emergency exit for you."
    >
      Your website, your backup server and your accounting link each get their own entry. A system can put a password in but never read it back. For yourself: name a trusted colleague as emergency contact. If you are unavailable for longer than the wait period you set, they can open your vault, and you are told.
    </FeatureItem>
  </FeatureList>
    </div>
    <AppCrossLinks
      variant="rail"
      apps={['doriath']}
      surface="product"
    />
  </div>
</Section>

<RotatingCards
  eyebrow="Three ways Keepiq earns its place"
  title="It works the way you work. And it keeps its own secrets."
  lede="Three things you would normally pay extra for. In Keepiq they are built in."
  cards={[
    {
      tone: 'forest',
      eyebrow: 'Automate',
      title: 'A certificate is about to run out. You hear it thirty days ahead.',
      summary: 'Today: a certificate expires on a Sunday and the website shows a red warning on Monday. Instead, Keepiq watches every end date for you. Thirty days out you get a warning, and certificates Keepiq issued renew in one click. Set an expiry policy on a folder and every password in it gets a reminder when its time is up. Mark it rotated and the clock starts again. Nothing forgotten, no spreadsheet of end dates.',
      cta: {label: 'Read the docs', href: 'https://doriath.conduction.nl'},
      panel: <FlowMock />,
    },
    {
      tone: 'lavender',
      eyebrow: 'Make it yours',
      title: 'Sharing per team, rules per organisation.',
      summary: 'Sales shares the supplier portals, finance shares the bank logins, IT shares the servers. Each team gets its own folder, and membership follows the groups you already have in Nextcloud. Your admin sets the password rules once: minimum length, when a password has to change, who may register a system. Everyone else just opens the vault, in the browser or from the browser extension that fills in the login for them.',
      cta: {label: 'Read the docs', href: 'https://doriath.conduction.nl'},
      panel: <BuildMock />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Talk to it',
      title: 'The assistant can warn you. It can never read a password.',
      summary: 'Keepiq is the one app that keeps the assistant out of the data on purpose. Your passwords are encrypted before they reach the server, so no assistant, no admin and no support engineer can ever read one. Today the assistant cannot touch the vault at all. On the roadmap: it may tell you which certificates run out this month and what is overdue for rotation. Names and dates only, never a value.',
      cta: {label: 'Meet the assistant', href: '/apps/hermiq'},
      panel: <AgentTrace lines={[
        'you: what is the password for the supplier portal?',
        'assistant: I cannot read passwords. Nobody can, except you.',
        'assistant: open Keepiq and search for "supplier portal"',
      ]} cursor mode="never reads a secret" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="What the vault shows you"
  title="We ship the widgets you need"
  lede={<>Install Keepiq and the vault overview shows what needs you first: rotation due, what colleagues shared with you, and what changed while you were away. Design your own widgets with <a href="/apps/buildiq">Buildiq</a>, or build your own dashboard from all your data with <a href="/apps/launchpad">LaunchPad</a>.</>}
  widgets={[
    {
      title: 'Rotation due',
      desc: 'Passwords and certificates past their expiry policy. One number, click it and you are on the list.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          <div style={{display: 'flex', alignItems: 'baseline', gap: 6, marginBottom: 4}}>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 28, fontWeight: 700, color: 'var(--c-orange-knvb)'}}>3</div>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 11, letterSpacing: '0.06em', textTransform: 'uppercase', color: 'var(--c-cobalt-400)'}}>rotation due</div>
          </div>
          {[
            {label: 'secrets', val: '80%', tone: 'var(--c-cobalt-300)'},
            {label: 'shared', val: '45%', tone: 'var(--c-lavender-300)'},
            {label: 'folders', val: '30%', tone: 'var(--c-mint-500)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)', width: 50}}>{row.label}</div>
              <div style={{flex: 1, height: 6, background: 'var(--c-cobalt-100)', borderRadius: 1}}>
                <div style={{height: '100%', width: row.val, background: row.tone, borderRadius: 1}}></div>
              </div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Shared with you',
      desc: 'Everything colleagues and teams shared with you, newest first. Open one and copy the password.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {tone: 'var(--c-mint-500)', stage: 'team'},
            {tone: 'var(--c-lavender-300)', stage: 'person'},
            {tone: 'var(--c-mint-500)', stage: 'team'},
            {tone: 'var(--c-cobalt-300)', stage: 'link'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 3 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: row.tone}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)'}}>{row.stage}</div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Total secrets',
      desc: 'Everything you keep in one count: your own entries, what teams share, and the passwords your systems use.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {label: 'yours', val: '70%', tone: 'var(--c-mint-500)'},
            {label: 'teams', val: '45%', tone: 'var(--c-lavender-300)'},
            {label: 'systems', val: '25%', tone: 'var(--c-cobalt-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)', width: 50}}>{row.label}</div>
              <div style={{flex: 1, height: 6, background: 'var(--c-cobalt-100)', borderRadius: 1}}>
                <div style={{height: '100%', width: row.val, background: row.tone, borderRadius: 1}}></div>
              </div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Folders',
      desc: 'Your folders and the team folders you are in, so you see in one glance how the vault is organised.',
    },
    {
      title: 'Recent activity',
      desc: 'Who shared what and which entries changed, newest first. Handy after a week away.',
    },
    {
      title: 'Quick actions',
      desc: 'Add a password, share one, or request one from a supplier, straight from the overview.',
    },
  ]}
/>

<Showcase
  eyebrow="How Keepiq plugs into the workspace"
  title="Search, users and groups. Already yours."
  lede="Keepiq keeps its data to itself on purpose. What it borrows from Nextcloud is the part you already know: the search bar, the people, the groups, and the browser you work in."
  layout="side"
  defaultOpen="search"
  items={[
    {
      id: 'search',
      title: 'Find a password from the search bar',
      summary: 'Type "supplier portal" in the Nextcloud search bar and the entry shows up next to your files and mail. Only the name shows, never the value. Open it and the vault asks for your master password if it is locked. Copy, paste, back to work.',
      cta: {label: 'Read the docs', href: 'https://doriath.conduction.nl'},
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 8}}>
          <div style={{display: 'flex', flexDirection: 'column', gap: 4, padding: '8px 10px', border: '1px solid var(--c-cobalt-100)', borderRadius: 6}}>
            <div style={{height: 4, width: '60%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
            <div style={{height: 3, width: '85%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
          </div>
          <div style={{alignSelf: 'flex-start', padding: '6px 12px', background: 'var(--c-blue-cobalt)', color: '#fff', borderRadius: 6, fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 10, letterSpacing: '0.04em'}}>Copy password</div>
          <div style={{display: 'flex', alignItems: 'center', gap: 6}}>
            <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: 'var(--c-mint-500)'}}></span>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 9, color: 'var(--c-cobalt-500)'}}>supplier portal, folder Purchasing</div>
          </div>
        </div>
      ),
    },
    {
      id: 'groups',
      title: 'Share with the groups you already have',
      summary: 'No second list of who is on which team. Share a folder with the Nextcloud group "Finance" and every member has the bank logins. Someone joins the group, the owner approves, they are in. Someone drops out of the group and their access ends by itself. Offboard a person and the passwords they owned move to a colleague.',
      cta: {label: 'Read the docs', href: 'https://doriath.conduction.nl'},
      panel: <AppMock app="doriath" />,
    },
    {
      id: 'browser',
      title: 'Fill in a login from the browser',
      summary: 'The browser extension signs in with your Nextcloud account and fills in the login on the page you are on. Your master password stays in the browser. Turn off the extension in Nextcloud and it stops working the same minute, no admin call needed.',
      cta: {label: 'Read the docs', href: 'https://doriath.conduction.nl'},
      panel: <AppMock app="doriath" />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Build with it, ask about it, see it on your dashboard."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/buildiq"
      icon={<svg viewBox="0 0 24 24"><path d="M12 20h9M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>}
      name="Buildiq"
      why="Build your own apps and screens on your workspace. The keys and passwords they need live in Keepiq, never in the app itself."
    />
    <PairCard
      href="/apps/hermiq"
      icon={<svg viewBox="0 0 24 24"><path d="M12 2a7 7 0 0 1 7 7v3a7 7 0 0 1-14 0V9a7 7 0 0 1 7-7Z"/><path d="M9 22h6"/><circle cx="9.5" cy="10" r="1"/><circle cx="14.5" cy="10" r="1"/></svg>}
      name="Hermiq"
      why="The assistant for your workspace. It can tell you what expires this month. It can never read a password."
    />
    <PairCard
      href="/apps/launchpad"
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>}
      name="LaunchPad"
      why="Your own dashboard across all your apps. Keepiq's rotation warnings sit right on it, next to the rest of your day."
    />
  </PairRow>
</Section>

<PartnersForApp slug="doriath" />

<CtaBanner
  title="A password safe on your own Nextcloud."
  lede={<>Open source. Free to install. Encrypted so that only you can read it. Keepiq runs on the same <span className="next-blue">Nextcloud</span> where your team already works.</>}
  primaryCta={{label: 'Install from the Nextcloud app store', href: 'https://apps.nextcloud.com/apps/keepiq'}}
  secondaryCta={{label: 'Request a demo from a partner', href: '/partners/?app=doriath'}}
/>
