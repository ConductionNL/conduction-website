---
title: PipelinQ, Conduction
description: CRM with clients, lead pipeline, quotes, and contactmomenten. Built on OpenRegister, no separate sales database, no extra login.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace, AppCrossLinks} from '@conduction/docusaurus-preset/components';
import {PartnersForApp} from '@site/src/components/PartnersFor';

<DetailHero
  background="cobalt"
  appId="pipelinq"
  crumb={[{label: 'Apps', href: '/apps'}, 'PipelinQ']}
  status={{label: 'Beta', color: 'var(--c-orange-knvb)'}}
  version="v0.5"
  locales="NL · EN"
  title="PipelinQ"
  tagline={<>CRM on your <span className="next-blue">Nextcloud</span>. Clients, contacts, leads, quotes. Everything as a typed register, no separate sales database, no second login. Your sales team works where your delivery team already is.</>}
  intro={<>Pipelinq is an open-source CRM for the Nextcloud workspace. It runs a drag-and-drop lead pipeline with configurable stages, values and close probabilities, a client and contact register with a full 360° history, service-request intake, and contactmoment logging. Clients, contacts, leads and requests are typed OpenRegister schemas. Released under EUPL-1.2 and maintained by Conduction.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://pipelinq.conduction.nl'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://codeberg.org/Conduction/pipelinq'}}
  illustration={<AppMock app="pipelinq" />}
/>

<Section spacing="default">
  <div style={{display: 'grid', gridTemplateColumns: 'minmax(0, 1fr) 320px', gap: 'var(--space-8)', alignItems: 'start'}}>
    <div style={{minWidth: 0}}>
      <SectionHead
        eyebrow="What it does"
        title="Clients, pipeline, quotes, contactmomenten — one CRM."
        align="stack"
        lede="PipelinQ manages clients and contact persons, tracks leads through a visual pipeline, turns quotes into contracts, and logs every contactmoment. Sales and operational dashboards sit on top, with scheduled CSV/reporting exports."
      />
      <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M9 3v18M15 8v10"/></svg>}
      title="Lead pipeline with a drag-and-drop kanban board."
    >
      Move leads through configurable stages with values and close probabilities. The Pipeline board and the Sales dashboard show pipeline-by-stage, revenue-over-time and win-rate live from the same records.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="4" y="3" width="16" height="18" rx="2"/><circle cx="12" cy="10" r="3"/><path d="M7 18a5 5 0 0 1 10 0"/></svg>}
      title="Clients and contacts as a typed register."
    >
      OpenRegister schemas for clients and contact persons, with a 360° client view (leads, requests, contactmomenten, projects, complaints). Two-way sync with the native Nextcloud Contacts app via CardDAV.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      title="Quotes that become the contract."
    >
      A quotation and its contract are one record — once it's accepted, the same lines, price and client carry over with no re-entry. Recurring-revenue metrics feed the dashboard once a contract is active.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>}
      title="Contactmomenten and reporting."
    >
      Log calls, emails, visits and other client interactions with an outcome and channel, and schedule CSV (or Parquet/JSONL) data exports for reporting outside Pipelinq.
    </FeatureItem>
  </FeatureList>
    </div>
    <AppCrossLinks
      variant="rail"
      apps={['pipelinq']}
      surface="product"
    />
  </div>
</Section>

<PartnersForApp slug="pipelinq" />

<RotatingCards
  eyebrow="Three ways PipelinQ earns its place"
  title="Pipeline, register-linked, dashboards built in."
  lede="Scroll the cards. Each pins as you reach it; the next slides up to cover."
  cards={[
    {
      tone: 'lavender',
      eyebrow: 'Lead pipeline',
      title: 'Drag leads through your stages.',
      summary: 'Configurable stages with values and close probabilities, a Pipeline kanban board, and quote-to-contract handling in the same record set.',
      cta: {label: 'See PipelinQ', href: '/apps/pipelinq'},
      panel: <AppMock app="pipelinq" />,
    },
    {
      tone: 'forest',
      eyebrow: 'Register-linked',
      title: 'Clients and leads as typed registers.',
      summary: 'Clients, contacts, leads and requests are typed OpenRegister schemas with an audit trail. The CRM stops being a silo, and duplicate-detection flags matching clients on save.',
      cta: {label: 'See OpenRegister', href: '/apps/openregister'},
      panel: <AppMock app="openregister" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Built-in dashboards',
      title: 'Sales and operational overviews out of the box.',
      summary: 'A Sales dashboard (revenue, win rate, weighted forecast) and an Operational dashboard (open leads, requests, resolution time) read the same registers live — no separate BI tool required.',
      cta: {label: 'See PipelinQ', href: '/apps/pipelinq'},
      panel: <AppMock app="pipelinq" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="Widgets we ship"
  title="Sales on the dashboard the team already opens."
  lede="Install PipelinQ and its Sales and Operational dashboards show up in the app. Pipeline value first, deals closing soon next, open requests below."
  widgets={[
    {
      title: 'Pipeline by stage',
      desc: 'Open pipeline value grouped by stage, plus a weighted-forecast KPI. Live, not nightly batch.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          <div style={{display: 'flex', alignItems: 'baseline', gap: 6, marginBottom: 4}}>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 24, fontWeight: 700, color: 'var(--c-cobalt-700)'}}>€ 487k</div>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 10, letterSpacing: '0.05em', color: 'var(--c-mint-500)'}}>+12%</div>
          </div>
          {[
            {label: 'lead', val: '40%', tone: 'var(--c-cobalt-300)'},
            {label: 'qualified', val: '60%', tone: 'var(--c-lavender-300)'},
            {label: 'proposal', val: '75%', tone: 'var(--c-mint-500)'},
            {label: 'won', val: '50%', tone: 'var(--c-forest-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)', width: 50}}>{row.label}</div>
              <div style={{flex: 1, height: 6, background: 'var(--c-cobalt-100)', borderRadius: 1}}>
                <div style={{height: '100%', width: row.val, background: row.tone, borderRadius: 1}}></div>
              </div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Deals closing soon',
      desc: 'Open leads sorted by expected close date, with client and value at a glance.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {tone: 'var(--c-mint-500)', stage: 'this week'},
            {tone: 'var(--c-lavender-300)', stage: 'next week'},
            {tone: 'var(--c-mint-500)', stage: 'this week'},
            {tone: 'var(--c-orange-knvb)', stage: 'overdue'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 3 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: row.tone}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)'}}>{row.stage}</div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Hot leads',
      desc: 'Top leads by value. Click through to the client record.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {val: '€ 84k', tone: 'var(--c-orange-knvb)'},
            {val: '€ 62k', tone: 'var(--c-mint-500)'},
            {val: '€ 48k', tone: 'var(--c-lavender-300)'},
            {val: '€ 31k', tone: 'var(--c-cobalt-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8}}>
              <span style={{width: 12, height: 14, clipPath: 'var(--hex-pointy-top)', background: row.tone, flexShrink: 0}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '65%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '45%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 10, fontWeight: 700, color: 'var(--c-cobalt-700)'}}>{row.val}</div>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How PipelinQ plugs into the workspace"
  title="Sales runs on the workspace your delivery team already uses."
  lede="Real integrations that keep PipelinQ inside Nextcloud — contacts, quotes-as-contracts, and a handoff to billing."
  layout="side"
  defaultOpen="contacts-sync"
  items={[
    {
      id: 'contacts-sync',
      title: 'Nextcloud Contacts sync',
      summary: 'Clients and contact persons sync two-way with the native Contacts app via CardDAV, so names, emails and phone numbers stay in one place.',
      cta: {label: 'Read the docs', href: 'https://pipelinq.conduction.nl'},
      panel: <AppMock app="pipelinq" />,
    },
    {
      id: 'quote-to-contract',
      title: 'One record from quote to contract',
      summary: 'A quotation and its contract share one record — accepting a quote carries the lines, price and client straight into the contract, with recurring-revenue metrics once it is active.',
      cta: {label: 'Read the docs', href: 'https://pipelinq.conduction.nl'},
      panel: <AppMock app="pipelinq" />,
    },
    {
      id: 'reporting-export',
      title: 'Scheduled CSV export',
      summary: 'Export leads, clients and contactmomenten on a schedule as CSV (or Parquet/JSONL) for reporting outside Pipelinq.',
      cta: {label: 'Read the docs', href: 'https://pipelinq.conduction.nl'},
      panel: <AppMock app="pipelinq" />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Two apps that complete the CRM workflow."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/procest"
      icon={<svg viewBox="0 0 24 24"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>}
      name="Procest"
      why="Case management for the direct request-to-case handoff. A dedicated bridge is in development."
    />
    <PairCard
      href="/apps/openregister"
      icon={<svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="8" ry="3"/><path d="M4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6"/></svg>}
      name="OpenRegister"
      why="The data foundation. Clients, leads and quotes as typed registers with an audit log."
    />
  </PairRow>
</Section>

<CtaBanner
  title="CRM on your own Nextcloud."
  lede={<>No separate SaaS, no second login. PipelinQ runs on the same <span className="next-blue">Nextcloud</span> where your delivery team already works.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Talk to a partner', href: '/partners'}}
/>
