---
title: SoftwareCatalog, Conduction
description: Software portfolio management on Nextcloud. Applications, modules, contracts, standards and compliance in one federated register.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace} from '@conduction/docusaurus-preset/components';
import {PartnersForApp} from '@site/src/components/PartnersFor';

<DetailHero
  background="cobalt"
  appId="softwarecatalog"
  crumb={[{label: 'Apps', href: '/apps'}, 'SoftwareCatalog']}
  status={{label: 'Beta', color: 'var(--c-orange-knvb)'}}
  version="v0.2"
  locales="NL · EN"
  title="SoftwareCatalog"
  tagline={<>Software portfolio management on your <span className="next-blue">Nextcloud</span>. A register of every application, module, contract, and standard your organisation runs. Built for the IT department that wants to know what runs where.</>}
  intro={<>SoftwareCatalog is an open-source software-portfolio app for the Nextcloud workspace. It keeps a register of applications, module versions, connections between them, contracts (with an approval workflow), and organisations, on top of OpenRegister. A standards register and compliance matrix track which modules satisfy which GEMMA standard, backed by evidence; a portfolio roadmap groups applications in use by lifecycle phase with end-of-support warnings. Catalogue entries can be published as open data, with anonymous self-registration and moderation for new organisations, and synced with other Software Catalogus instances. Released under EUPL-1.2 and maintained by Conduction.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://softwarecatalog.conduction.nl'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://codeberg.org/Conduction/softwarecatalog'}}
  illustration={<AppMock app="softwarecatalog" />}
/>

<Section spacing="default">
  <SectionHead
    eyebrow="What it does"
    title="One register, every install."
    align="stack"
    lede="SoftwareCatalog tracks which applications and modules your organisation runs, under which contract, against which standards. OpenCatalogi on top makes the whole catalogue searchable across the organisation."
  />
  <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/></svg>}
      title="Applications, modules, and connections."
    >
      Register applications, break them into modules and module versions, and map the connections (koppelingen) between them. One OpenRegister-backed catalogue per organisation.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      title="Contract administration with approval."
    >
      Track contract period, cost, and status (active, expiring, in negotiation) with a submit-and-approve workflow for new contracts and renewals.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>}
      title="GEMMA standards, compliance matrix, ArchiMate."
    >
      Maintain a standards register, record compliance claims with evidence per module, and cross-check modules against standard versions in a compliance matrix. Import and export the underlying architecture model as ArchiMate (AMEF) XML.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
      title="Federated sync and open-data publishing."
    >
      Sync catalogue data with other Software Catalogus instances (add a peer, pull on demand). Publish selected entries as open data for transparency and reuse; anonymous organisations can self-register through a moderated intake queue.
    </FeatureItem>
  </FeatureList>
</Section>

<PartnersForApp slug="softwarecatalog" />

<RotatingCards
  eyebrow="Three ways SoftwareCatalog earns its place"
  title="Register, assess, federate."
  lede="Scroll the cards. Each pins as you reach it; the next slides up to cover."
  cards={[
    {
      tone: 'lavender',
      eyebrow: 'Register',
      title: 'Applications, modules, contracts, organisations.',
      summary: 'One OpenRegister-backed catalogue for applications, module versions, connections, contracts, and the organisations behind them. Contact roles link back to real Nextcloud accounts.',
      cta: {label: 'See SoftwareCatalog', href: '/apps/softwarecatalog'},
      panel: <AppMock app="softwarecatalog" />,
    },
    {
      tone: 'forest',
      eyebrow: 'Assess',
      title: 'Standards, compliance matrix, portfolio roadmap.',
      summary: 'A standards register plus compliance claims with evidence, cross-tabbed in a compliance matrix. A per-organisation roadmap groups applications in use by lifecycle phase with end-of-support warnings.',
      cta: {label: 'See SoftwareCatalog', href: '/apps/softwarecatalog'},
      panel: <AppMock app="softwarecatalog" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Federate',
      title: 'Sync with other catalogue instances.',
      summary: 'Federated sync (via OpenCatalogi\'s directory network, when installed) mirrors organisation profiles between instances. Publish selected entries as open data, with a moderated intake queue for anonymous self-registration.',
      cta: {label: 'See OpenCatalogi', href: '/apps/opencatalogi'},
      panel: <AppMock app="opencatalogi" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="On the dashboard"
  title="Object counts, at a glance."
  lede="Install SoftwareCatalog and the dashboard shows a management-information panel plus per-object-type statistics tables, split across organisations, applications, contracts, standards and more."
  widgets={[
    {
      title: 'Object statistics',
      desc: 'A count per object type in the register — organisations, applications, module versions, contracts, standards, compliance claims, reviews.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {label: 'organisaties', val: '70%', tone: 'var(--c-cobalt-300)'},
            {label: 'applicaties', val: '55%', tone: 'var(--c-mint-500)'},
            {label: 'contracten', val: '40%', tone: 'var(--c-lavender-300)'},
            {label: 'standaarden', val: '30%', tone: 'var(--c-forest-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)', width: 70}}>{row.label}</div>
              <div style={{flex: 1, height: 5, background: 'var(--c-cobalt-100)', borderRadius: 1}}>
                <div style={{height: '100%', width: row.val, background: row.tone, borderRadius: 1}}></div>
              </div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Management information',
      desc: 'A summary panel with links straight into organisations, contracts, and standards — the entry points an IT- or portfolio-lead uses most.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {tone: 'var(--c-cobalt-300)', label: 'Organisations'},
            {tone: 'var(--c-mint-500)', label: 'Contracts'},
            {tone: 'var(--c-lavender-300)', label: 'Standards'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 2 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: row.tone}}></span>
              <div style={{flex: 1, height: 4, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How SoftwareCatalog plugs into the workspace"
  title="Contracts, standards, and federation, on top of OpenRegister."
  lede="Three integration paths that keep the catalogue useful beyond a single instance. Most of the day-to-day plumbing rides on OpenRegister and, optionally, OpenCatalogi."
  layout="side"
  defaultOpen="contracts"
  items={[
    {
      id: 'contracts',
      title: 'Contract approval workflow',
      summary: 'New contracts and renewals go through a submit-and-approve step before they take effect, with quick filters for active, expiring, and in-negotiation contracts, and a background job that keeps status current.',
      cta: {label: 'View on GitHub', href: 'https://codeberg.org/Conduction/softwarecatalog'},
      panel: <AppMock app="softwarecatalog" />,
    },
    {
      id: 'standards',
      title: 'GEMMA standards & ArchiMate',
      summary: 'Record which modules satisfy which GEMMA standard, with evidence documents, and view it as a compliance matrix. Import and export the underlying architecture model as ArchiMate (AMEF) XML for round-trip use with modelling tools.',
      cta: {label: 'View on GitHub', href: 'https://codeberg.org/Conduction/softwarecatalog'},
      panel: <AppMock app="softwarecatalog" />,
    },
    {
      id: 'federation',
      title: 'Federated sync via OpenCatalogi',
      summary: 'When OpenCatalogi is installed, SoftwareCatalog delegates to its directory network to mirror organisation profiles between instances and to publish selected entries as open data. Without OpenCatalogi it degrades cleanly to a single-instance catalogue.',
      cta: {label: 'See OpenCatalogi', href: '/apps/opencatalogi'},
      panel: <AppMock app="opencatalogi" />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Two apps that extend the catalogue."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/openregister"
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/></svg>}
      name="OpenRegister"
      why="The register foundation SoftwareCatalog is built on. Required dependency for every install."
    />
    <PairCard
      href="/apps/opencatalogi"
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
      name="OpenCatalogi"
      why="Optional: powers federated sync between catalogue instances and open-data publishing."
    />
  </PairRow>
</Section>

<CtaBanner
  title="What runs where, under which contract, against which standard."
  lede={<>SoftwareCatalog keeps applications, contracts, and GEMMA compliance in one register, so the answer is a click away instead of a spreadsheet hunt.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Talk to a partner', href: '/partners'}}
/>
