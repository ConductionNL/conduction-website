---
title: SoftwareCatalog, Conduction
description: See exactly which software you run, and which you can retire. Applications, connections, contracts and standards in one place, on your own Nextcloud.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace, AppCrossLinks} from '@conduction/docusaurus-preset/components';
import {PartnersForApp} from '@site/src/components/PartnersFor';

<DetailHero
  background="cobalt"
  appId="softwarecatalog"
  crumb={[{label: 'Apps', href: '/apps'}, 'SoftwareCatalog']}
  status={{label: 'Beta', color: 'var(--c-orange-knvb)'}}
  version="v0.2"
  locales="NL · EN"
  title="SoftwareCatalog"
  tagline={<>See exactly which software you run, and which you can retire. Every application, contract and standard in one place, on your own <span className="next-blue">Nextcloud</span>.</>}
  intro={<>SoftwareCatalog is an open-source software portfolio app for the Nextcloud workspace. Built for the IT department that wants to know what runs where, and under which contract. Record every application and the connections between them. Track contract end dates and yearly cost. Show with evidence which system meets which standard. Hear in time before a vendor stops support. Municipalities get the Dutch reference architecture for local government built in. Released under EUPL-1.2 and maintained by Conduction.</>}
  primaryCta={{label: 'Install from the Nextcloud app store', href: '/install'}}
  secondaryCta={{label: 'Request a demo from a partner', href: '/partners'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://github.com/ConductionNL/softwarecatalog'}}
  illustration={<AppMock app="softwarecatalog" />}
/>

<Section spacing="default">
  <div style={{display: 'grid', gridTemplateColumns: 'minmax(0, 1fr) 320px', gap: 'var(--space-8)', alignItems: 'start'}}>
    <div style={{minWidth: 0}}>
      <SectionHead
        eyebrow="What it does"
        title="What runs where, under which contract, against which standard."
        align="stack"
        lede="SoftwareCatalog answers the questions that today take a spreadsheet hunt. Which systems talk to which. Which contract ends next. Which system meets which standard, and where the proof is. Which software falls out of support first."
      />
      <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/></svg>}
      title="Which system talks to which."
    >
      Record every application, its versions and the connections between them. Replacing the HR package? The connections on record show which systems touch it. You know what breaks before you sign, not after.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      title="A contract ends in ninety days. You hear now."
    >
      Every contract carries its end date, yearly cost and status. Expiring contracts sit in one filter and can notify you. A renewal goes through a recorded decision before it takes effect. Not after the auto-renew invoice arrives.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>}
      title="The auditor asks which systems meet standard X. One screen answers."
    >
      Record per system which standard it meets, with the evidence attached. The compliance matrix crosses your systems against the standards you pick. Verified with proof, claimed, or missing, per cell.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 3"/></svg>}
      title="You hear a year before the vendor stops support."
    >
      Every version carries its end-of-support date. The portfolio roadmap groups your systems by lifecycle phase, most urgent first. A reported vulnerability shows which of your systems it hits, and who to warn.
    </FeatureItem>
  </FeatureList>
    </div>
    <AppCrossLinks
      variant="rail"
      apps={['softwarecatalog']}
      surface="product"
    />
  </div>
</Section>

<PartnersForApp slug="softwarecatalog" />

<RotatingCards
  eyebrow="Three ways SoftwareCatalog earns its place"
  title="It works the way you work. And it works for you."
  lede="Three things you would normally pay extra for. In SoftwareCatalog they are built in."
  cards={[
    {
      tone: 'forest',
      eyebrow: 'Automate',
      title: 'A contract nears its end. The decision is already on the agenda.',
      summary: 'Today: someone finds the auto-renew invoice in March and asks who agreed to it. Instead, set it up once. Ninety days before a contract ends, the owner hears. The renewal waits for a recorded decision before it takes effect. When a version passes its end-of-support date, the roadmap flags it and the owner is warned. A newly reported vulnerability warns the people responsible for the systems it hits. Nothing slips, no spreadsheet to keep, no coding.',
      cta: {label: 'How automation works', href: '/apps/openregister'},
      panel: <AppMock app="openregister" />,
    },
    {
      tone: 'lavender',
      eyebrow: 'Make it yours',
      title: 'Your portfolio, your terms. Without a developer.',
      summary: 'One organisation tracks data classification on every application. Another wants the budget holder on every contract. A shared service centre needs the member municipality on every usage. Add the field, add the filter. Drag the view you check first to the top. Hide what you never use. In the running app, in minutes, and your changes stay with every update.',
      cta: {label: 'How customising works', href: '/apps/openbuild'},
      panel: <AppMock app="openbuild" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Talk to it',
      title: 'Ask the catalogue a question. Get the list, not a report to build.',
      summary: 'Ask "which contracts end this year?" and get the list with amounts and owners. Ask "which systems still run the old version?" and the answer is there. Ask "who is the contact for the tax package?" and you have a name. The assistant reads what the catalogue publishes and stays within what you allowed it. Nothing in the catalogue changes without your say-so.',
      cta: {label: 'Meet the assistant', href: '/apps/hermiq'},
      panel: <AgentTrace lines={[
        'you: which contracts end this year?',
        'assistant: 4 contracts, 212,000 euro a year in total',
        'assistant: the HR package ends 30 November, owner is Els',
        'you: which systems depend on the HR package?',
        'assistant: 4 connections: payroll, identity, time registration, intranet',
      ]} cursor mode="reads, never edits, your catalogue" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="On the dashboard"
  title="The three questions your IT lead asks first."
  lede="Install SoftwareCatalog and the dashboard opens with the portfolio in numbers and the shortcuts an IT or portfolio lead uses most. Contracts, standards, organisations, one click each."
  widgets={[
    {
      title: 'Portfolio in numbers',
      desc: 'How many applications you run, how many contracts carry them, how many standards you track. Click a row and you are in the list.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {label: 'apps', val: '70%', tone: 'var(--c-cobalt-300)'},
            {label: 'contracts', val: '55%', tone: 'var(--c-mint-500)'},
            {label: 'standards', val: '40%', tone: 'var(--c-lavender-300)'},
            {label: 'vendors', val: '30%', tone: 'var(--c-forest-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)', width: 70}}>{row.label}</div>
              <div style={{flex: 1, height: 5, background: 'var(--c-cobalt-100)', borderRadius: 1}}>
                <div style={{height: '100%', width: row.val, background: row.tone, borderRadius: 1}}></div>
              </div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Contracts ending soon',
      desc: 'Open the contract list on the expiring filter: end date, yearly cost and status per contract. The renewal decision starts from here.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {tone: 'var(--c-orange-knvb)', stage: '41 days'},
            {tone: 'var(--c-lavender-300)', stage: '68 days'},
            {tone: 'var(--c-mint-500)', stage: '85 days'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 2 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: row.tone}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)'}}>{row.stage}</div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Portfolio roadmap',
      desc: 'Your systems by lifecycle phase, most urgent first. End of support, phase-out and planned replacement on one timeline, per organisation.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {val: 'EOL', tone: 'var(--c-orange-knvb)'},
            {val: '2027', tone: 'var(--c-lavender-300)'},
            {val: '2028', tone: 'var(--c-mint-500)'},
            {val: 'ok', tone: 'var(--c-cobalt-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8}}>
              <span style={{width: 12, height: 14, clipPath: 'var(--hex-pointy-top)', background: row.tone, flexShrink: 0}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '65%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '45%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 10, fontWeight: 700, color: 'var(--c-cobalt-700)'}}>{row.val}</div>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How SoftwareCatalog plugs into the workspace"
  title="Files, decisions, accounts and other catalogues. Already yours."
  lede="The catalogue lives where your documents, your decisions and your colleagues already live. That is the point of a workspace."
  layout="side"
  defaultOpen="files"
  items={[
    {
      id: 'files',
      title: 'The contract and the evidence, on the record',
      summary: 'The signed contract, the vendor documentation, the audit evidence behind a compliance claim: on the record, not in someone\'s mailbox. Every application and contract keeps its documents in Files. When the auditor asks for proof, it is one click away.',
      cta: {label: 'Read the docs', href: 'https://softwarecatalog.conduction.nl'},
      panel: <AppMock app="softwarecatalog" />,
    },
    {
      id: 'decisions',
      title: 'A renewal is a recorded decision',
      summary: 'A new contract or a renewal does not just switch to active. It goes to DeciDesk as a decision and gets its sign-off there. The outcome lands back on the contract. Who approved what, and when, is on the record two years later.',
      cta: {label: 'See DeciDesk', href: '/apps/decidesk'},
      panel: <AppMock app="decidesk" />,
    },
    {
      id: 'accounts',
      title: 'Contacts become colleagues with the right role',
      summary: 'Add a vendor contact or an application owner and they get a Nextcloud account with the right role. The vendor sees only what they offer, never another organisation\'s landscape. No second user list to keep in sync.',
      cta: {label: 'Read the docs', href: 'https://softwarecatalog.conduction.nl'},
      panel: <AppMock app="softwarecatalog" />,
    },
    {
      id: 'federation',
      title: 'Learn from what other organisations run',
      summary: 'Publish your software choices as open data, without private details. See which systems you run together with other organisations. With OpenCatalogi installed, catalogues discover and share with each other. A municipality sees what its neighbours chose before it decides. Without it, the catalogue simply stays yours.',
      cta: {label: 'See OpenCatalogi', href: '/apps/opencatalogi'},
      panel: <AppMock app="opencatalogi" />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Two apps that make the catalogue count."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/decidesk"
      icon={<svg viewBox="0 0 24 24"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>}
      name="DeciDesk"
      why="Contract renewals go through a recorded decision. Who approved what is on the record."
    />
    <PairCard
      href="/apps/opencatalogi"
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
      name="OpenCatalogi"
      why="Publish your software choices as open data and see what other organisations run."
    />
  </PairRow>
</Section>

<CtaBanner
  title="Know what runs where, on your own Nextcloud."
  lede={<>Open source. Free to install. Your portfolio stays on your own server. SoftwareCatalog runs on the same <span className="next-blue">Nextcloud</span> where your team already works.</>}
  primaryCta={{label: 'Install from the Nextcloud app store', href: '/install'}}
  secondaryCta={{label: 'Request a demo from a partner', href: '/partners'}}
/>
