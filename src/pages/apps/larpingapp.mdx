---
title: LarpingApp, Conduction
description: Character, event and rules management for live-action role-play (LARP) groups. Typed registers, audit trail, PDF character sheets and GM run-sheets.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace, AppCrossLinks} from '@conduction/docusaurus-preset/components';
import {PartnersForApp} from '@site/src/components/PartnersFor';

<DetailHero
  background="cobalt"
  appId="larpingapp"
  crumb={[{label: 'Apps', href: '/apps'}, 'LarpingApp']}
  status={{label: 'Beta', color: 'var(--c-orange-knvb)'}}
  version="v0.1"
  locales="NL · EN"
  title="LarpingApp"
  tagline={<>Characters, rules, and events for live-action role-play (LARP). Build a setting in <span className="next-blue">Nextcloud</span>, share it with your group, and run sessions without spreadsheets-with-six-tabs.</>}
  intro={<>LarpingApp is an open-source live-action role-playing (LARP) management app for the Nextcloud workspace. Game masters design characters, abilities, skills, items, conditions and effects as typed records, and the app recalculates every stat automatically as players gain experience or effects change. It also handles event rosters, background-approval workflows, audited post-event XP awards, and printable PDF character sheets and GM run-sheets — giving game masters one place to run a campaign. Released under EUPL-1.2 and maintained by Conduction since 2019.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://larpingapp.conduction.nl'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://codeberg.org/Conduction/larpingapp'}}
  illustration={<AppMock app="larpingapp" />}
/>

<Section spacing="default">
  <div style={{display: 'grid', gridTemplateColumns: 'minmax(0, 1fr) 320px', gap: 'var(--space-8)', alignItems: 'start'}}>
    <div style={{minWidth: 0}}>
      <SectionHead
        eyebrow="What it does"
        title="A LARP setting that grows with your campaign."
        align="stack"
        lede="Characters, abilities, skills, items, conditions and effects, all as typed OpenRegister records, linked to each other, version-safe. Players edit their own character in the same Nextcloud where you run the campaign. No export-import-shuffle between tools."
      />
      <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="12" cy="8" r="4"/><path d="M4 21a8 8 0 0 1 16 0"/></svg>}
      title="Character sheets with an approval workflow."
    >
      A character is a typed record: stats, background story, skills, items and a portrait. Players submit backgrounds for game-master review before the character enters play.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M12 2l3 6 6 1-4.5 4.5 1 6-5.5-3-5.5 3 1-6L3 9l6-1z"/></svg>}
      title="A stat engine you configure."
    >
      Abilities, skills, items, conditions and effects are all typed registers. The stat engine recalculates every ability deterministically as skills are learned, items equipped, or conditions applied — with prerequisite checks enforced at the write boundary.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/><rect x="3" y="16" width="7" height="5"/></svg>}
      title="Settings scope your campaigns."
    >
      Group characters, events and mechanics under a setting (world/campaign). Finished campaigns are archived, not deleted, so past rosters stay readable.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M3 6h18M3 12h18M3 18h12"/></svg>}
      title="Audited XP awards."
    >
      Game masters grant experience per event, per character, with a reason and a timestamp. Every award is a recorded, auditable ledger entry — not a spreadsheet cell.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M21 12a9 9 0 1 1-9-9"/><path d="M21 3v6h-6"/></svg>}
      title="PDF sheets and GM run-sheets."
    >
      Export a printable character sheet, or a per-event cast list with every attendee's stats, conditions and approval state, rendered through DocuDesk.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M12 2L2 7v6c0 5 3 9 10 11 7-2 10-6 10-11V7L12 2z"/></svg>}
      title="Open source, EUPL-1.2."
    >
      No yearly SaaS licence for the small organiser. Self-host on a Nextcloud you already have, or run via a Conduction partner.
    </FeatureItem>
  </FeatureList>
    </div>
    <AppCrossLinks
      variant="rail"
      apps={['larpingapp']}
      surface="product"
    />
  </div>
</Section>

<PartnersForApp slug="larpingapp" />

<RotatingCards
  eyebrow="Three ways LarpingApp earns its place"
  title="Characters, rules, players, all in one workspace."
  lede="Scroll the cards. Each pins as you reach it; the next slides up to cover."
  cards={[
    {
      tone: 'lavender',
      eyebrow: 'Characters',
      title: 'Sheets with an approval workflow.',
      summary: 'Every character is a typed record: stats, background, skills, items, a portrait. Players submit a background for game-master review; approved characters carry their computed XP and gold into every event.',
      cta: {label: 'See LarpingApp', href: '/apps/larpingapp'},
      panel: <AppMock app="larpingapp" />,
    },
    {
      tone: 'forest',
      eyebrow: 'Rules',
      title: 'Abilities, skills, items, conditions, effects.',
      summary: 'Every game mechanic is a typed, cross-linked register. The stat engine recalculates abilities deterministically as effects apply, with prerequisite checks enforced server-side before a character write is accepted.',
      cta: {label: 'See OpenRegister', href: '/apps/openregister'},
      panel: <AppMock app="openregister" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Players',
      title: 'Each player gets their own dashboard.',
      summary: 'LaunchPad gives every player a personal view: their character stats, recent events, upcoming sessions. One Nextcloud login for the whole campaign, no separate player portal.',
      cta: {label: 'See LaunchPad', href: '/apps/launchpad'},
      panel: <AppMock app="launchpad" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="Widgets we ship"
  title="Your campaign on the Nextcloud home screen."
  lede="Install LarpingApp and these dashboard widgets show up out of the box: character and event counts, recent characters, recent events, and skill usage across the roster."
  widgets={[
    {
      title: 'Characters, events, items, players',
      desc: 'Four KPI stat tiles on the dashboard, each linking straight to its index page.',
      panel: (
        <div style={{display: 'flex', gap: 10, alignItems: 'flex-start'}}>
          <div style={{width: 44, height: 50, clipPath: 'var(--hex-pointy-top)', background: 'var(--c-lavender-300)', flexShrink: 0}}></div>
          <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 6}}>
            <div style={{height: 6, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
            <div style={{height: 4, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
            {[{label: 'CHAR', val: '60%'}, {label: 'EVT', val: '80%'}, {label: 'ITEM', val: '45%'}, {label: 'PLYR', val: '70%', accent: true}].map((row, i) => (
              <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
                <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 9, letterSpacing: '0.05em', color: 'var(--c-cobalt-400)', width: 26}}>{row.label}</div>
                <div style={{flex: 1, height: 4, background: 'var(--c-cobalt-100)', borderRadius: 1, position: 'relative'}}>
                  <div style={{height: '100%', width: row.val, background: row.accent ? 'var(--c-mint-500)' : 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                </div>
              </div>
            ))}
          </div>
        </div>
      ),
    },
    {
      title: 'Recent events',
      desc: 'The latest events by start date, with a one-click link to each event roster.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {date: '14', tone: 'var(--c-mint-500)'},
            {date: '21', tone: 'var(--c-lavender-300)'},
            {date: '28', tone: 'var(--c-forest-300)'},
            {date: '04', tone: 'var(--c-cobalt-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8}}>
              <div style={{width: 22, height: 25, clipPath: 'var(--hex-pointy-top)', background: row.tone, display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0}}>
                <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 9, fontWeight: 700, color: 'var(--c-cobalt-700)'}}>{row.date}</div>
              </div>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 3}}>
                <div style={{height: 4, width: '75%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '55%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Recent characters & skill usage',
      desc: 'The newest character sheets by name and type, plus a chart of skill adoption across the roster.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {['Jack Doe — Warrior', 'Elenna Sway — Scout', 'Baldor Ash — Cleric', 'Vey Marrow — Rogue'].map((_, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 3 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: i === 0 ? 'var(--c-orange-knvb)' : 'var(--c-mint-500)'}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{height: 3, width: 22, background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How LarpingApp plugs into the workspace"
  title="The campaign lives where the group already does."
  lede="No separate player portal. LarpingApp reuses Nextcloud's own building blocks for scheduling, files and contacts instead of reinventing them."
  layout="side"
  defaultOpen="calendar"
  items={[
    {
      id: 'calendar',
      title: 'Calendar, maps and sign-up',
      summary: 'Each event schedules through Nextcloud Calendar from its start/end date, with location and sign-up form as sidebar tabs. The organiser sets it up once; players see it on the Nextcloud they already log in to.',
      cta: {label: 'See the docs', href: 'https://larpingapp.conduction.nl'},
      panel: <AppMock app="larpingapp" />,
    },
    {
      id: 'files',
      title: 'Files and Photos',
      summary: 'Character portraits and sheets live in the Photos integration; item props, handouts and event packs attach through Files. Versioning is Nextcloud-native, no extra tooling.',
      cta: {label: 'See how to organise the campaign', href: 'https://larpingapp.conduction.nl'},
      panel: <AppMock app="openregister" />,
    },
    {
      id: 'contacts',
      title: 'Contacts and OpenRegister',
      summary: 'Each player links to their Nextcloud contact card for phone, email and address. All game data — characters, events, mechanics — is stored as OpenRegister objects with a full audit trail and versioning.',
      cta: {label: 'See OpenRegister', href: '/apps/openregister'},
      panel: <AppMock app="openregister" />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Two apps that carry the campaign."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/openregister"
      icon={<svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="8" ry="3"/><path d="M4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6"/></svg>}
      name="OpenRegister"
      why="Characters, abilities, skills, items and events as typed registers, each with a full audit trail and versioning."
    />
    <PairCard
      href="/apps/launchpad"
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/><rect x="3" y="16" width="7" height="5"/></svg>}
      name="LaunchPad"
      why="Per-player dashboard with your character stats, recent events, and upcoming sessions."
    />
  </PairRow>
</Section>

<CtaBanner
  title="A LARP without spreadsheets."
  lede={<>Install LarpingApp from the <span className="next-blue">Nextcloud</span> app store, import your existing characters into a schema, and run the next session without tab-shuffle.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'View on GitHub', href: 'https://codeberg.org/Conduction/larpingapp'}}
/>
