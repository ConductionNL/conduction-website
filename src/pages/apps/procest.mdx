---
title: Procest, Conduction
description: Case-management for VTH and citizen processes. Workflow engine plus typed registers, on Nextcloud.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace} from '@conduction/docusaurus-preset/components';
import {PartnersForApp} from '@site/src/components/PartnersFor';

<DetailHero
  background="cobalt"
  appId="procest"
  crumb={[{label: 'Apps', href: '/apps'}, 'Procest']}
  status={{label: 'Beta', color: 'var(--c-orange-knvb)'}}
  version="v0.2.40"
  locales="NL · EN"
  title="Procest"
  tagline={<>Case management for VTH (permits, supervision, enforcement) and citizen processes on your <span className="next-blue">Nextcloud</span>. A workflow engine plus typed registers plus an audit trail that meets the Wet kwaliteitsborging and Algemene wet bestuursrecht.</>}
  intro={<>Procest is an open-source case-management app for the Nextcloud workspace, built for Dutch government organisations. Case workers list and filter cases, drive them through ZGW-compliant case-type definitions, and pick up tasks from a personal work queue. The app maps ZGW API fields onto OpenRegister, so cases interoperate with national exchange standards without a second database. It targets zaakgericht werken scenarios such as complaints, consultations, and WOO disclosure requests. Released under EUPL-1.2 and maintained by Conduction since 2019.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://procest.conduction.nl'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://codeberg.org/Conduction/procest'}}
  illustration={<AppMock app="procest" />}
/>

<Section spacing="default">
  <SectionHead
    eyebrow="What it does"
    title="Workflow plus typed records, one install."
    align="stack"
    lede="Procest ships with process templates for the most common VTH cases (permits, inspections, enforcement requests). It combines a lightweight workflow engine with OpenRegister for the data and TMLO-tagged archival; decision generation and signing plug in via adapters, with DocuDesk as an optional target."
  />
  <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 3"/></svg>}
      title="Pre-defined VTH process templates."
    >
      Omgevingsvergunning, toezichtzaak (bouw/milieu), handhavingszaak, sloopmelding — plus bezwaar &amp; beroep and WOO-verzoek case types. All configurable without code.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M9 11l3 3 8-8"/><path d="M20 12v6a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h9"/></svg>}
      title="Statutory deadlines, watched automatically."
    >
      Statutory deadlines are baked into the process templates. Procest warns in advance, flags missed deadlines, and logs everything to the audit trail.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      title="Decisions, archived with TMLO metadata."
    >
      Click the "grant permit" transition and Procest composes the decision from the case data. PDF generation and signing plug in via an adapter (DocuDesk optional); archival with retention/TMLO metadata runs natively on OpenRegister.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M3 17l6-6 4 4 8-8"/><path d="M14 3h7v7"/></svg>}
      title="Workflow board, case map, and appointments."
    >
      A kanban-style workflow board next to the case list, a map view for location-bound cases, and citizen-facing appointment booking with timeslots.
    </FeatureItem>
  </FeatureList>
</Section>

<PartnersForApp slug="procest" />

<RotatingCards
  eyebrow="Three ways Procest earns its place"
  title="Stage timeline, register-linked, in every queue."
  lede="Scroll the cards. Each pins as you reach it; the next slides up to cover."
  cards={[
    {
      tone: 'lavender',
      eyebrow: 'Stage timeline',
      title: 'Every case has a visible workflow.',
      summary: 'Procest runs ZGW-shaped case management. Five stages, one active, the rest queued or done. Citizens see the same status the case-worker sees, only the actions differ.',
      cta: {label: 'See Procest', href: '/apps/procest'},
      panel: <AppMock app="procest" />,
    },
    {
      tone: 'forest',
      eyebrow: 'Register-linked',
      title: 'Every case is a typed register row.',
      summary: 'Stages, roles, dossiers, archive entries are all OpenRegister records. Audit trail is automatic, retention follows the schema, citation IDs survive every export.',
      cta: {label: 'See OpenRegister', href: '/apps/openregister'},
      panel: <AppMock app="openregister" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'On every queue',
      title: 'Case widgets on every dashboard.',
      summary: 'Every case-worker gets a set of case widgets on their LaunchPad — cases overview, deadline alerts, overdue cases, and their own task list. No separate inbox app.',
      cta: {label: 'See LaunchPad', href: '/apps/launchpad'},
      panel: <AppMock app="launchpad-widgets" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="Widgets we ship"
  title="On every Nextcloud dashboard."
  lede="Install Procest and these widgets show up on the home screen of every case-worker. No extra config, no separate inbox app."
  widgets={[
    {
      title: 'Cases overview',
      desc: 'Active cases for the logged-in case-worker. Late ones in red, the next stage one click away.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6, fontFamily: 'var(--conduction-typography-font-family-body)'}}>
          {[
            {tone: 'var(--c-orange-knvb)', l1: '60%', l2: '40%', av: 'var(--c-mint-300)'},
            {tone: 'var(--c-lavender-300)', l1: '70%', l2: '50%', av: 'var(--c-lavender-300)'},
            {tone: 'var(--c-red-vermillion)', l1: '55%', l2: '45%', av: 'var(--c-orange-knvb)'},
            {tone: 'var(--c-lavender-300)', l1: '65%', l2: '35%', av: 'var(--c-mint-300)'},
            {tone: 'var(--c-lavender-300)', l1: '75%', l2: '50%', av: 'var(--c-forest-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8}}>
              <span style={{width: 14, height: 16, clipPath: 'var(--hex-pointy-top)', background: row.tone, flexShrink: 0}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 3}}>
                <div style={{height: 5, width: row.l1, background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: row.l2, background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <span style={{width: 18, height: 18, borderRadius: '50%', background: row.av, flexShrink: 0}}></span>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Deadline alerts',
      desc: 'Cases with statutory deadlines hitting today or this week. Pre-warning, then escalation.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          <div style={{display: 'flex', alignItems: 'baseline', gap: 6, marginBottom: 4}}>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 28, fontWeight: 700, color: 'var(--c-orange-knvb)'}}>3</div>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 11, letterSpacing: '0.06em', textTransform: 'uppercase', color: 'var(--c-cobalt-400)'}}>this week</div>
          </div>
          {[{w: '70%', accent: true}, {w: '60%'}, {w: '55%'}].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
              <span style={{width: 8, height: 9, clipPath: 'var(--hex-pointy-top)', background: row.accent ? 'var(--c-orange-knvb)' : 'var(--c-cobalt-300)'}}></span>
              <div style={{flex: 1, height: 4, background: 'var(--c-cobalt-200)', borderRadius: 1, width: row.w}}></div>
              <div style={{height: 4, width: 30, background: 'var(--c-cobalt-100)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Overdue cases',
      desc: 'Cases past their deadline, oldest first. Linked back to the source register row.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {['Vergunning · 3 dagen', 'Handhaving · 1 dag', 'Vergunning · 5 dagen', 'Melding · 2 dagen'].map((_, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 3 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: i === 1 ? 'var(--c-red-vermillion)' : 'var(--c-orange-knvb)'}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{height: 3, width: 22, background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How Procest plugs into the workspace"
  title="Cases everywhere your team already works."
  lede="Three integration paths that pull Procest into the rest of the Nextcloud workspace and the workflow tools your team brings."
  layout="side"
  defaultOpen="mail-files"
  items={[
    {
      id: 'mail-files',
      title: 'Mail and Files',
      summary: 'Inbound citizen email lands on the case automatically. Attachments file themselves under the case dossier; the case-worker sees the new mail in the case timeline without leaving the case view.',
      cta: {label: 'See the integration guide', href: 'https://procest.conduction.nl/mail-files'},
      panel: <AppMock app="procest" />,
    },
    {
      id: 'n8n',
      title: 'Automatic actions via n8n',
      summary: 'Trigger n8n webhooks on case events — a new case, a status change, an inbound email — to auto-handle the simple cases and route the rest to the right team.',
      cta: {label: 'See the workflow examples', href: 'https://procest.conduction.nl/n8n-complaint-workflows'},
      panel: <AppMock app="openconnector" />,
    },
    {
      id: 'ai-assistance',
      title: 'AI-assisted case processing',
      summary: 'An Ollama-compatible LLM endpoint classifies inbound documents, extracts structured data, drafts summaries, and suggests routing — the case worker reviews and confirms every suggestion.',
      cta: {label: 'See the AI assistance docs', href: 'https://procest.conduction.nl'},
      panel: <AgentTrace
        lines={[
          {kind: 'note',  bullet: 'done', text: 'Inbound document parsed'},
          {kind: 'tool',  text: 'procest - classify_document(file: aanvraag-2026-118.pdf)'},
          {kind: 'note',  bullet: 'info', text: 'Classified as "omgevingsvergunning"', muted: '(confidence 0.91)'},
          {kind: 'status', text: 'Routing suggestion ready for case-worker review'},
        ]}
        cursor
        mode="review required (no auto-apply)"
      />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Three apps that complete the VTH workflow."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/zaakafhandelapp"
      icon={<svg viewBox="0 0 24 24"><rect x="4" y="4" width="16" height="16" rx="2"/><path d="M9 9h6v6H9z"/></svg>}
      name="ZaakAfhandelApp"
      why="A companion ZGW-aligned case app for citizen-facing case handling — same terminology, same Nextcloud workspace."
    />
    <PairCard
      href="/apps/docudesk"
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      name="DocuDesk"
      why="Beschikkingen, ontvangstbevestigingen, bezwaarbrieven uit zaak-velden."
    />
    <PairCard
      href="/apps/openregister"
      icon={<svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="8" ry="3"/><path d="M4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6"/></svg>}
      name="OpenRegister"
      why="Cases, decisions, inspections as typed registers. Audit trail per record."
    />
  </PairRow>
</Section>

<CtaBanner
  title="VTH on your own Nextcloud."
  lede={<>Procest ships with process models for the most common VTH cases. Deadlines are watched, decision documents get generated, the audit trail covers everything.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Talk to a partner', href: '/partners'}}
/>
