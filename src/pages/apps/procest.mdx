---
title: Procest, Conduction
description: Case management for permits, objections, complaints and enforcement, on your own Nextcloud. Every legal deadline counted from day one, warned about in time, and written down.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace, AppCrossLinks} from '@conduction/docusaurus-preset/components';
import {PartnersForApp} from '@site/src/components/PartnersFor';

<DetailHero
  background="cobalt"
  appId="procest"
  crumb={[{label: 'Apps', href: '/apps'}, 'Procest']}
  status={{label: 'Beta', color: 'var(--c-orange-knvb)'}}
  version="v0.3.9"
  locales="NL · EN"
  title="Procest"
  tagline={<>Case management for permits, objections, complaints and enforcement, on your own <span className="next-blue">Nextcloud</span>. Every legal deadline counted from day one.</>}
  intro={<>Procest is open-source case management for Dutch municipalities and other public bodies. Case workers see their cases and tasks in one place. Residents follow their own case. Every legal deadline is counted, warned about and written down. It follows the Dutch case-management exchange standard (ZGW), so it fits the systems you already run. Released under EUPL-1.2 and maintained by Conduction.</>}
  primaryCta={{label: 'Install from the Nextcloud app store', href: '/install'}}
  secondaryCta={{label: 'Request a demo from a partner', href: '/partners'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://github.com/ConductionNL/procest'}}
  illustration={<AppMock app="procest" />}
/>

<Section spacing="default">
  <div style={{display: 'grid', gridTemplateColumns: 'minmax(0, 1fr) 320px', gap: 'var(--space-8)', alignItems: 'start'}}>
    <div style={{minWidth: 0}}>
      <SectionHead
        eyebrow="What it does"
        title="Cases, deadlines, decisions, residents. One case system."
        align="stack"
        lede="Procest handles building permits, inspections, enforcement, demolition notices, objections and appeals, complaints and open government requests (Woo). You set up each case type yourself. The deadline clock, the decision and the file come with it."
      />
      <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 3"/></svg>}
      title="The deadline that never gets missed."
    >
      A permit has eight weeks by law. Procest starts counting on day one. Two weeks before the end the case worker hears about it. At one week the team leader hears too. At two days the manager gets the red flag. If the day passes anyway, the case is marked overdue, everyone on it is told, and it is written down that this happened. No permit is granted by default because someone was on leave. When you ask the applicant for more information, the clock pauses. When a decision does run late, Procest calculates the penalty the law prescribes.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M3 17l6-6 4 4 8-8"/><path d="M14 3h7v7"/></svg>}
      title="Case types you set up yourself."
    >
      Statuses, deadlines, documents and who signs off: you draw the process on a canvas. A functional administrator changes it. No developer, no waiting for a release.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="4" y="3" width="16" height="18" rx="2"/><circle cx="12" cy="10" r="3"/><path d="M7 18a5 5 0 0 1 10 0"/></svg>}
      title="Residents see what you see."
    >
      The resident opens their case and sees the same status the case worker sees. The "where is my permit?" calls stop. When an inspection is needed, they pick a slot themselves through a public link, and it lands in the inspector's calendar.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      title="The full file, two years later."
    >
      An objection arrives two years after the decision. One click opens the whole file: every document, every status change, every decision and who took it. Export it for the objections committee or the court in one go.
    </FeatureItem>
  </FeatureList>
    </div>
    <AppCrossLinks
      variant="rail"
      apps={['procest']}
      surface="product"
    />
  </div>
</Section>

<PartnersForApp slug="procest" />

<RotatingCards
  eyebrow="Three ways Procest earns its place"
  title="It works the way you work. And it works for you."
  lede="Three things you would normally pay extra for. In Procest they are built in."
  cards={[
    {
      tone: 'forest',
      eyebrow: 'Automate',
      title: 'A case is ready for decision. The sign-off is already on its way.',
      summary: 'Today: the case worker finishes the assessment, then walks the file to whoever may sign. Instead, set it up once. The moment a case reaches "ready for decision", the sign-off task is on the desk of the person with the mandate. A permit above your threshold waits for a second pair of eyes before anything goes out. A case that has not moved in three weeks turns up on the team leader\'s list on Monday. Nothing forgotten, no chasing, no coding.',
      cta: {label: 'How automation works', href: '/apps/openregister'},
      panel: <AppMock app="openregister" />,
    },
    {
      tone: 'lavender',
      eyebrow: 'Make it yours',
      title: 'Your case types, your fields, your screens.',
      summary: 'A small municipality wants the neighbourhood on every case. An environmental service wants the inspector\'s photos at the top of the file. A water board never handles objections and wants that page out of the menu. Add the field, move the block, hide what you never use. In the running app, in minutes, and your changes stay with every update.',
      cta: {label: 'How customising works', href: '/apps/openbuild'},
      panel: <AppMock app="openbuild" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Talk to it',
      title: 'Ask which cases are open. Get the list, not a report.',
      summary: 'Ask "which cases are still in intake?" and you get the list. Ask "what happened on case 2026-0042?" and you get the history, step by step, with who did what. Ask which deadlines fall this week and the assistant reads the same clock the dashboard shows. It reads. Changing a case stays with the case worker.',
      cta: {label: 'Meet the assistant', href: '/apps/hermiq'},
      panel: <AgentTrace lines={[
        'you: which cases are still in intake?',
        'assistant: 6 cases in intake, oldest 11 days',
        'you: what happened on 2026-0042?',
        'assistant: received 3 March, assigned 4 March, more information requested 12 March',
        'assistant: clock paused since 12 March, 34 days left',
      ]} cursor mode="reads only, never changes a case" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="Widgets we ship"
  title="Deadlines, overdue cases and your tasks. On the dashboard."
  lede="Install Procest and every case worker sees the deadlines coming up, the cases already late and their own tasks the moment they log in. No separate inbox."
  widgets={[
    {
      title: 'Deadline alerts',
      desc: 'Cases whose legal deadline falls this week. The one that needs you today is on top.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          <div style={{display: 'flex', alignItems: 'baseline', gap: 6, marginBottom: 4}}>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 28, fontWeight: 700, color: 'var(--c-orange-knvb)'}}>3</div>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 11, letterSpacing: '0.06em', textTransform: 'uppercase', color: 'var(--c-cobalt-400)'}}>this week</div>
          </div>
          {[{w: '70%', accent: true}, {w: '60%'}, {w: '55%'}].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
              <span style={{width: 8, height: 9, clipPath: 'var(--hex-pointy-top)', background: row.accent ? 'var(--c-orange-knvb)' : 'var(--c-cobalt-300)'}}></span>
              <div style={{flex: 1, height: 4, background: 'var(--c-cobalt-200)', borderRadius: 1, width: row.w}}></div>
              <div style={{height: 4, width: 30, background: 'var(--c-cobalt-100)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Overdue',
      desc: 'Cases past their deadline, oldest first. Click one and you are on the case.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {['permit', 'enforcement', 'permit', 'notice'].map((_, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 3 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: i === 1 ? 'var(--c-red-vermillion)' : 'var(--c-orange-knvb)'}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{height: 3, width: 22, background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'My tasks',
      desc: 'Your own tasks across all cases, due date first. Tick one off and the case moves on.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6, fontFamily: 'var(--conduction-typography-font-family-body)'}}>
          {[
            {tone: 'var(--c-orange-knvb)', l1: '60%', l2: '40%', av: 'var(--c-mint-300)'},
            {tone: 'var(--c-lavender-300)', l1: '70%', l2: '50%', av: 'var(--c-lavender-300)'},
            {tone: 'var(--c-red-vermillion)', l1: '55%', l2: '45%', av: 'var(--c-orange-knvb)'},
            {tone: 'var(--c-lavender-300)', l1: '65%', l2: '35%', av: 'var(--c-mint-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8}}>
              <span style={{width: 14, height: 16, clipPath: 'var(--hex-pointy-top)', background: row.tone, flexShrink: 0}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 3}}>
                <div style={{height: 5, width: row.l1, background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: row.l2, background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <span style={{width: 18, height: 18, borderRadius: '50%', background: row.av, flexShrink: 0}}></span>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How Procest plugs into the workspace"
  title="Mail, calendar, contacts and files. Already yours."
  lede="The case lives where your email, your calendar and your documents already live. That is the point of a workspace."
  layout="side"
  defaultOpen="mail"
  items={[
    {
      id: 'mail',
      title: 'The email on the case',
      summary: 'A resident replies to your request for more information. Open the message in Nextcloud Mail, link it to the case, and it sits in the case file next to the documents. Reply from a template and the case number, the name and the dates are already filled in as a draft. With DocuDesk installed, linked mail is archived to the file as a document, so it is still there when the objection comes.',
      cta: {label: 'Read the docs', href: 'https://procest.conduction.nl'},
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 8}}>
          <div style={{display: 'flex', flexDirection: 'column', gap: 4, padding: '8px 10px', border: '1px solid var(--c-cobalt-100)', borderRadius: 6}}>
            <div style={{height: 4, width: '75%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
            <div style={{height: 3, width: '55%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
            <div style={{height: 3, width: '85%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
          </div>
          <div style={{alignSelf: 'flex-start', padding: '6px 12px', background: 'var(--c-blue-cobalt)', color: '#fff', borderRadius: 6, fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 10, letterSpacing: '0.04em'}}>Link to case</div>
          <div style={{display: 'flex', alignItems: 'center', gap: 6}}>
            <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: 'var(--c-mint-500)'}}></span>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 9, color: 'var(--c-cobalt-500)'}}>linked to case 2026-0042</div>
          </div>
        </div>
      ),
    },
    {
      id: 'calendar',
      title: 'Hearings and appointments in the calendar',
      summary: 'Plan the hearing or the site visit from the case and it is in your Nextcloud Calendar, linked to the case. Let residents book their own inspection slot: Procest offers the free slots and puts the confirmed appointment in the inspector\'s agenda. No phone tag to find a date.',
      cta: {label: 'Read the docs', href: 'https://procest.conduction.nl'},
      panel: <AppMock app="procest" />,
    },
    {
      id: 'contacts',
      title: 'The people on the case are your contacts',
      summary: 'The applicant, the objector, the neighbour who complained: every person on a case is a Nextcloud contact, connected to the case. Look up who is involved without opening a second address book. The phone number on the case is the one in your contacts.',
      cta: {label: 'Read the docs', href: 'https://procest.conduction.nl'},
      panel: <AppMock app="procest" />,
    },
    {
      id: 'files',
      title: 'The drawings and the decision, in the file',
      summary: 'Every case keeps its own folder in Files. The drawings, the site photos, the decision letter: on the case, searchable, with every version kept. Found a drawing in Files and wonder which case it belongs to? It tells you, and takes you there.',
      cta: {label: 'Read the docs', href: 'https://procest.conduction.nl'},
      panel: <AppMock app="procest" />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Two apps that finish what a case starts."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/decidesk"
      icon={<svg viewBox="0 0 24 24"><path d="M12 2v4M12 18v4M2 12h4M18 12h4"/><circle cx="12" cy="12" r="4"/></svg>}
      name="DeciDesk"
      why="A case that needs a formal decision by the council or board gets it there, and the case shows the outcome and its history."
    />
    <PairCard
      href="/apps/shillinq"
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="16" rx="2"/><path d="M3 10h18M8 15h4"/></svg>}
      name="Shillinq"
      why="Calculate the fee for a permit from your own tariff and bill it, without retyping the case."
    />
  </PairRow>
</Section>

<CtaBanner
  title="Case management on your own Nextcloud."
  lede={<>Open source. Free to install. Your case files stay on your own server. Procest runs on the same <span className="next-blue">Nextcloud</span> where your team already works.</>}
  primaryCta={{label: 'Install from the Nextcloud app store', href: '/install'}}
  secondaryCta={{label: 'Request a demo from a partner', href: '/partners'}}
/>
