---
title: DocuDesk, Conduction
description: Generate, anonymise, sign, and template documents on Nextcloud. Templates ship for the most-used Dutch government documents.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace, AppCrossLinks} from '@conduction/docusaurus-preset/components';
import {PartnersForApp} from '@site/src/components/PartnersFor';

<DetailHero
  background="cobalt"
  appId="docudesk"
  crumb={[{label: 'Apps', href: '/apps'}, 'DocuDesk']}
  status={{label: 'Beta', color: 'var(--c-orange-knvb)'}}
  version="v0.0.34"
  locales="NL · EN"
  title="DocuDesk"
  tagline={<>Document generation, anonymisation, signing, and templating on <span className="next-blue">Nextcloud</span>. Starter templates ship for common Dutch government documents (beschikkingen, brieven, kwartaalrapportages). Pulls fields straight from your registers.</>}
  intro={<>DocuDesk is an open-source document-processing app for the Nextcloud workspace. It generates PDF, ODF (.odt), or HTML documents from Twig templates, anonymizes personal data through a configurable entity-detection backend, signs documents with eIDAS signature levels, and keeps a full audit trail for GDPR and WOO publication consent. All processing runs locally on the customer's Nextcloud instance, so sensitive files never leave the premises. Released under EUPL-1.2 and maintained by Conduction since 2019.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docudesk.conduction.nl'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://codeberg.org/Conduction/docudesk'}}
  illustration={<AppMock app="docudesk" />}
/>

<Section spacing="default">
  <div style={{display: 'grid', gridTemplateColumns: 'minmax(0, 1fr) 320px', gap: 'var(--space-8)', alignItems: 'start'}}>
    <div style={{minWidth: 0}}>
      <SectionHead
        eyebrow="What it does"
        title="Generate, anonymise, sign. From the same template."
        align="stack"
        lede="DocuDesk reads register fields straight into Twig templates and renders PDF, ODF, or HTML output, anonymises documents before they reach your registers, and signs outbound documents with eIDAS signature levels. One install covers the whole document lifecycle."
      />
      <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      title="Templates with register-aware fields."
    >
      Drop a register's schema in, DocuDesk auto-binds the fields. Twig/HTML templates render as PDF, ODF, or HTML. Regenerate any time, registers stay the system of record.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="12" cy="8" r="4"/><path d="M5 21v-2a4 4 0 0 1 4-4h6a4 4 0 0 1 4 4v2"/></svg>}
      title="PII anonymisation, configurable backend."
    >
      Entity detection routed through OpenRegister to a configurable backend — regex, OpenAnonymiser, Presidio, or an LLM. Drop a document, get a redacted version that's safe for the register.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M9 11l3 3 8-8"/><path d="M20 12v6a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h9"/></svg>}
      title="eIDAS digital signing."
    >
      SES, AdES, and QES signature levels, sequential or parallel multi-signer workflows. Signing requests, signer records, and audit entries are stored as OpenRegister objects.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M21 11.5a8.4 8.4 0 0 1-9 8.4 8.4 8.4 0 0 1-3.4-.7L3 21l1.5-5.4A8.4 8.4 0 1 1 21 11.5z"/></svg>}
      title="Starter templates, extend your own."
    >
      Ships with example templates for common government documents (beschikking, algemene brief, kwartaalrapportage). Wet open overheid-aware; add your own templates per register schema.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M12 2v4M12 18v4M2 12h4M18 12h4M5 5l3 3M16 16l3 3M5 19l3-3M16 8l3-3"/></svg>}
      title="Audit log per document."
    >
      Every generation, edit, signing, archival action leaves a tamper-evident trail. Compliance evidence ships with the install.
    </FeatureItem>
  </FeatureList>
    </div>
    <AppCrossLinks
      variant="rail"
      apps={['docudesk']}
      surface="product"
    />
  </div>
</Section>

<PartnersForApp slug="docudesk" />

<RotatingCards
  eyebrow="Three ways DocuDesk earns its place"
  title="Generate, anonymise, sign, on the same registers."
  lede="Scroll the cards. Each pins as you reach it; the next slides up to cover."
  cards={[
    {
      tone: 'terracotta',
      eyebrow: 'Generate',
      title: 'Templates that read your registers.',
      summary: 'Drop a register schema into a Twig/HTML template, DocuDesk auto-binds the fields and renders PDF, ODF, or HTML. Regenerate any time. Starter templates ship with the install; extend with your own.',
      cta: {label: 'See DocuDesk', href: '/apps/docudesk'},
      panel: <AppMock app="docudesk" />,
    },
    {
      tone: 'forest',
      eyebrow: 'Anonymise',
      title: 'PII stripped before it lands.',
      summary: 'A configurable entity-detection backend — regex, OpenAnonymiser, Presidio, or an LLM — routed through OpenRegister. Documents are redacted before they touch a register. Citation-stable IDs survive the redaction.',
      cta: {label: 'See OpenRegister', href: '/apps/openregister'},
      panel: <AppMock app="openregister" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Sign + audit',
      title: 'eIDAS signing, full audit trail.',
      summary: 'Documents sign with SES, AdES, or QES signature levels through sequential or parallel signer workflows. Every generation, edit, signing action leaves a tamper-evident entry in the audit log.',
      cta: {label: 'See LaunchPad', href: '/apps/launchpad'},
      panel: <AppMock app="launchpad-bi" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="Widgets we ship"
  title="On every Nextcloud dashboard."
  lede="Install DocuDesk and these widgets show up on the home screen of every team member who handles documents."
  widgets={[
    {
      title: 'Anonymise dropzone',
      desc: 'Drop an inbound email or file here. DocuDesk strips PII before the document touches the register.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 8, alignItems: 'stretch'}}>
          <div style={{height: 90, border: '2px dashed var(--c-cobalt-300)', borderRadius: 'var(--radius-md)', background: 'var(--c-cobalt-50)', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 8}}>
            <span style={{width: 28, height: 32, clipPath: 'var(--hex-pointy-top)', background: 'var(--c-terracotta-500)'}}></span>
            <div style={{height: 4, width: 80, background: 'var(--c-cobalt-400)', borderRadius: 1}}></div>
          </div>
          <div style={{display: 'flex', alignItems: 'center', gap: 6}}>
            <span style={{width: 8, height: 9, clipPath: 'var(--hex-pointy-top)', background: 'var(--c-mint-500)'}}></span>
            <div style={{flex: 1, height: 3, background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
            <div style={{height: 3, width: 26, background: 'var(--c-cobalt-100)', borderRadius: 1}}></div>
          </div>
        </div>
      ),
    },
    {
      title: 'Pending signatures',
      desc: 'Documents awaiting your signature, sorted by deadline. One click to sign at the required eIDAS level.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {accent: true,  l1: '70%'},
            {accent: false, l1: '60%'},
            {accent: false, l1: '55%'},
            {accent: false, l1: '65%'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 3 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 12, height: 14, clipPath: 'var(--hex-pointy-top)', background: row.accent ? 'var(--c-orange-knvb)' : 'var(--c-cobalt-300)'}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 3}}>
                <div style={{height: 4, width: row.l1, background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '40%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{height: 18, width: 18, borderRadius: 3, background: row.accent ? 'var(--c-orange-knvb)' : 'var(--c-cobalt-50)', border: row.accent ? 'none' : '1px solid var(--c-cobalt-200)'}}></div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Recent documents',
      desc: 'Last documents you generated or signed. Type pill (PDF / ODF), filename, and a one-click reopen.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {pill: 'PDF',  pillBg: 'var(--c-terracotta-100)', pillFg: 'var(--c-terracotta-700)'},
            {pill: 'ODF',  pillBg: 'var(--c-cobalt-100)',     pillFg: 'var(--c-cobalt-700)'},
            {pill: 'PDF',  pillBg: 'var(--c-terracotta-100)', pillFg: 'var(--c-terracotta-700)'},
            {pill: 'HTML', pillBg: 'var(--c-mint-100)',       pillFg: 'var(--c-mint-700)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8}}>
              <span style={{display: 'inline-block', padding: '2px 6px', background: row.pillBg, color: row.pillFg, borderRadius: 2, fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 9, fontWeight: 700, letterSpacing: '0.04em'}}>{row.pill}</span>
              <div style={{flex: 1, height: 4, background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              <div style={{height: 3, width: 22, background: 'var(--c-cobalt-100)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How DocuDesk plugs into the workspace"
  title="One install, every document path."
  lede="Three integration paths that put DocuDesk between the Nextcloud apps your team already uses and the LLM provider you trust."
  layout="side"
  defaultOpen="mail-files"
  items={[
    {
      id: 'files',
      title: 'Files and dossiers',
      summary: 'Anonymise a single document, or scan a whole folder as a batch. Each redacted file is linked back to its source with a queryable OpenRegister record, and a per-dossier grondslagen (legal-basis) summary tracks which entities were redacted and why.',
      cta: {label: 'Read the docs', href: 'https://docudesk.conduction.nl'},
      panel: <AppMock app="docudesk" />,
    },
    {
      id: 'windmill-n8n',
      title: 'Windmill and n8n',
      summary: 'Trigger document generation as a workflow step. Windmill or n8n pulls fields from a register, calls DocuDesk\'s API to render the template, and routes the signed PDF wherever the flow wants.',
      cta: {label: 'See OpenConnector', href: '/apps/openconnector'},
      panel: <AppMock app="openconnector" />,
    },
    {
      id: 'entity-detection',
      title: 'Configurable entity detection',
      summary: 'PII detection runs through a backend you choose via OpenRegister — deterministic regex, OpenAnonymiser, Microsoft Presidio, or an LLM. Every redaction is written to the audit log with the detected entity type and the operator or automation that approved it.',
      cta: {label: 'See OpenRegister', href: '/apps/openregister'},
      panel: <AgentTrace
        lines={[
          {kind: 'note',  bullet: 'done', text: 'Document received'},
          {kind: 'tool',  text: 'entityDetection - scan(file: subsidie-aanvraag.pdf, backend: presidio)'},
          {kind: 'note',  bullet: 'info', text: 'Detected 7 PII spans', muted: '(BSN, PERSON, ADDRESS)'},
          {kind: 'status', text: 'Writing redacted version, sealing original'},
        ]}
        cursor
        mode="audit log on (every redaction recorded)"
      />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Three apps that compound the install."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/openregister"
      icon={<svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="8" ry="3"/><path d="M4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6"/></svg>}
      name="OpenRegister"
      why="The data foundation. DocuDesk templates bind to register schema fields directly."
    />
    <PairCard
      href="/apps/openconnector"
      icon={<svg viewBox="0 0 24 24"><circle cx="6" cy="12" r="3"/><circle cx="18" cy="6" r="3"/><circle cx="18" cy="18" r="3"/><path d="M9 12h9M9 12l9-6M9 12l9 6"/></svg>}
      name="OpenConnector"
      why="Routes inbound mail and outbound signed documents in and out of your zaaksysteem."
    />
    <PairCard
      href="/apps/opencatalogi"
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
      name="OpenCatalogi"
      why="Publishes generated documents to the public catalogue when they're WOO-disclosed."
    />
  </PairRow>
</Section>

<CtaBanner
  title="One install, the whole document lifecycle."
  lede={<>Install DocuDesk from the <span className="next-blue">Nextcloud</span> app store, point it at your registers, drop in the template pack. Your team is generating WOO-ready documents the same morning.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docudesk.conduction.nl'}}
/>
